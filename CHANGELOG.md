# Changelog

secureconfig is published continuously and versions as `1.0.<pull request number>`, where the number is
that of the most recently merged pull request. The current value is in the `VERSION` file at the
repository root. There is no release artifact, so the version names a state of `main` rather than a
downloadable build, and each guide remains dated by its own "Sources (checked <month year>)" section.
Entries here are grouped by the date the change landed on `main`. Note that nothing in the gate suite
can check `VERSION` against GitHub: the suite is deliberately offline so that no outside service can
turn the build red, and a pull request number is only knowable from outside. Keeping `VERSION` in step
with the merged pull request is therefore an authoring obligation, not an enforced one.

## 2026-09-17
- Hardened `open-webui.md` against a two-family, source-verified audit (#175; codex gpt-6-astra full-source returned DO NOT SHIP with a HIGH plus two MEDIUM and a LOW and source-verified every env-var claim, while claude-opus-4-8 was network-blocked and raised two HIGH suspicions from recall that codex's live checks and an independent orchestrator WebFetch both refuted). codex's HIGH: the `docker run` command omitted the `-v open-webui:/app/backend/data` volume the vendor requires, so replacing the container (an upgrade starts a fresh one) resets the database and, by the guide's own first-account-becomes-admin rule, hands the administrator seat to the next registrant if the public proxy keeps routing; the command now mounts the volume, and the guide says to reuse it and to keep the public route disabled until the admin account exists and signup is confirmed off. The `ss -tlnp` note now accounts for Docker's NAT (an absent host listener does not prove the container port is unreachable) and requires Docker Engine 28.0 or newer for loopback publishing (older engines let a same-L2 host reach a localhost-published port), the confirmed `127.0.0.1:3000->8080/tcp` mapping, and an external-host probe. Tailscale was moved out of the "login in front" grouping: Tailscale Funnel is public and adds no login, so the guide now points to Tailscale Serve (restricted by tailnet policy) or a Cloudflare Tunnel with Access, and says to keep Open WebUI's own authentication on behind Funnel (matching the sibling `tailscale.md`). The Verify HTTPS probe gains `--noproxy '*'`, `-g`, `-sS`, and a shown status so a client proxy cannot answer for the origin, a programmatic signup-state check against `/api/config` is added (confirm the signup feature flag is false), and the manual browser check now requires an actual registration refusal after the admin exists, not merely an absent sign-up button. Both families (codex live, orchestrator-reconfirmed by WebFetch) verified the accuracy core correct and unchanged: `ENABLE_SIGNUP` default `true` and its `ConfigVar` persistence, `DEFAULT_USER_ROLE` default `pending`, `ENABLE_PERSISTENT_CONFIG` default `true`, the `WEBUI_ADMIN_EMAIL`/`WEBUI_ADMIN_PASSWORD` first-launch admin bootstrap, `ENABLE_PASSWORD_AUTH=false` disabling password login for SSO-only operation (claude's recalled `ENABLE_LOGIN_FORM` is a different, UI-only variable), the first-account-becomes-admin rule, the Docker image and `3000:8080` loopback mapping, and all internal links. The claude family's two recall-based HIGH suspicions (that `ENABLE_PASSWORD_AUTH` and the `WEBUI_ADMIN_*` pair were invented) were deliberately not acted on: live source verification refuted both, and acting on recall over source would have broken correct fail-open controls.
- Hardened `mcp-servers.md` against a two-family, source-verified audit (#174; codex gpt-6-astra full-source returned DO NOT SHIP with four Verify defects, claude-opus-4-8 at high effort with network access returned DO NOT SHIP on the stale spec version plus two Verify-rigor gaps, and an independent orchestrator check confirmed the guide pins to a superseded MCP spec revision). The guide targets MCP `2025-11-25` and dates its Sources "checked September 2026", but the current revision is `2026-07-28` (verified at the MCP versioning page): every security control the guide teaches (Origin validation, loopback binding, the OAuth 2.1 / RFC 9728 / PKCE / RFC 8707 / audience-validation / no-token-passthrough rules) carries over unchanged, but 2026-07-28 removes protocol-level sessions and the `MCP-Session-Id` header, removes the standalone GET stream, and replaces the `initialize`-handshake version negotiation with a per-request `_meta` field, an `MCP-Protocol-Version` header, and a mandatory `server/discover` RPC, so the guide now carries an explicit spec-revision note pinning it to 2025-11-25, flagging 2026-07-28 as current with those transport changes, and pointing readers to the current specification (a full re-pin to 2026-07-28 is tracked as a follow-up). The Verify section was hardened: the unauthenticated-initialize, metadata-discovery, and wrong-Origin probes gain `--noproxy '*'` and `-g` so a client `HTTPS_PROXY` cannot answer with its own 401/407/403 in place of the target (the wrong-Origin false-pass was the real risk); the wrong-Origin check now isolates the Origin control with a positive control (an allowed Origin plus a valid initialize body must return a 2xx result) before changing only the Origin to a disallowed value and requiring 403, instead of posting an invalid `{}` body whose 403 could come from insufficient scope; the metadata probe now follows the `resource_metadata` URL the 401 advertises (with the endpoint-specific then root fallback) rather than assuming the root well-known path; the bearer credential moves off the command line into a mode-0600 `-H @` header file per `secrets.md` rule 5 (the same file convention machine-auth.md and python.md use), and the wrong-audience check is marked reasoned per CONTRIBUTING rule 5 with its command shape, prerequisite, and expected outcomes. Both families confirmed the rest correct and unchanged in 2026-07-28: every MCP-spec MUST/SHOULD/OPTIONAL quote is verbatim (the two transports and deprecated HTTP+SSE, the Origin-403 requirement, the OAuth 2.1 resource-server role, RFC 9728 metadata and `authorization_servers`, both discovery mechanisms, PKCE S256 and the `code_challenge_methods_supported` refusal, the RFC 8707 `resource`, audience validation and 401/403, no-token-passthrough and the separate upstream token, and the non-authenticating-sessions rule), the Azure `WEBSITE_AUTH_PRM_DEFAULT_WITH_SCOPES` preview setting, the nginx `if`/`map` Origin blocks (only `return` inside `if`, and the empty-key match for a missing Origin), the guarded loopback LAN probe, and all fourteen internal cross-links. The one divergence (codex flagged the argv bearer token, claude called it house style) was resolved at source in codex's favor: secrets.md rule 5 forbids secrets on the command line, and machine-auth.md:58, paas.md:38, and python.md:99 already use the `-H @file` convention.
- Hardened `vector-databases.md` against a two-family, source-verified audit (#173; codex gpt-6-astra full-source returned DO NOT SHIP on a single LOW Verify item and source-verified every version and default claim, while claude-opus-4-8 was network-blocked this session, ran a clean offline internal-consistency and Verify-reasoning review with no defect, and honestly marked the external claims NOT-LIVE-VERIFIED rather than dressing recall as verification; the two families agreed on the one actionable finding, and codex's live checks resolved every claim claude could not reach, including claude's highest-risk "possibly invented `alt_api_key`", which codex confirmed exists as of Qdrant v1.17.0). The Weaviate and Chroma quick-checks (`curl -q -si https://... | head -1`) did not disable client proxies, so with an HTTPS forward proxy configured the `head -1` line could show the proxy's `200 Connection established` CONNECT response instead of the service's status, and they lacked the status write-out and positive control the guide's own guarded Qdrant blocks already use; they now disable client proxies with `--noproxy '*'`, print `http=%{http_code}`/`exit=%{exitcode}` instead of piping to `head -1`, add `-g`, and add a valid-key positive control read from a file, so the checks cannot pass merely because nothing answered (the denial is accepted from Weaviate or the fronting proxy, not over-attributed to one layer). Both families confirmed the rest correct: the curl exit-code discrimination (7 connect-fail, 28 timeout, 6 DNS, 3 malformed, 52 empty reply, 56 receive failure) and the `http=000` ambiguity paired with the exit code, the plaintext-probe-to-a-TLS-only-port reasoning (the TCP handshake completes regardless of TLS), that the client-API key check cannot catch an exposed internal 6335 (6333 answers 401 then 200 identically either way), the single-node-with-cluster-enabled 6335 caveat, and the pgvector row-level-security positive control (superuser/`BYPASSRLS`/owner-unless-`FORCE ROW LEVEL SECURITY`, with the top-k caveat). codex additionally source-verified the version-gated claims that are the guide's accuracy core: Qdrant `enforce_internal_auth` (added v1.18.0, off by default, per the v1.18.0 release notes) and `alt_api_key` (available v1.17.0, per the security page), Weaviate anonymous-access default `true` and RBAC generally available from v1.29, the Milvus standalone compose publishing the bundled MinIO `9000`/`9001` on every interface (tagged compose source) and the default `root`/`Milvus`, the Chroma v1.0.0 removal of built-in authentication, pgvector on PostgreSQL 13 and later, and that every Sources URL resolves.
- Hardened `nodejs.md` against a two-family, source-verified audit (#172; codex gpt-6-astra full-source returned DO NOT SHIP with nine findings, and claude-opus-4-8 at high effort returned DO NOT SHIP with a subset of four, the two families agreeing on every substantive item). The guide told readers to "bind the Node app to `127.0.0.1`" and its `ss` Verify note expected loopback only, with no managed-platform case, so a reader deploying to Render, Railway, Fly, or Heroku produced an app the platform ingress cannot reach and might then bolt on self-TLS that `paas.md` forbids; mirroring the sibling `python.md` #171 fix, the intro and section 1 now carve out platform-terminated ingress (bind `0.0.0.0` on `$PORT` for Render, Railway, and Heroku Cedar; `::` on `$PORT` for Heroku Fir; `0.0.0.0` matching `internal_port` for Fly), note that the section-1 all-interface `listen(443)`/`listen(80)` is intended only for the Node-terminates-TLS layout, add the `paas.md` link, and the `ss` note references the platform address and port, with application authentication still required. The Verify authentication probe (`curl -q -s https://example.com/api # expect 401/403`) printed the body not the status, used a placeholder path that on most apps 404s (or passes while the real data route is open), disabled no client proxy, and had no positive control; it now disables client proxies with `--noproxy '*'`, shows the status, targets a real protected path (single-quoted), accepts a denial from whatever layer protects the route rather than over-claiming the app's own, and adds a valid-session positive control read from a cookie file (not argv) so the check cannot pass while auth is simply broken for everyone. All four Verify `curl` probes gain `--noproxy '*'`, since `-q` alone suppresses only `.curlrc` and not the `http_proxy`/`HTTPS_PROXY` environment proxies that would otherwise answer for the target and corrupt attribution; the `/whoami` forwarded-header probe also notes an overwriting proxy can mask a too-broad `trust proxy`, so a clean result there does not by itself prove the trust boundary. The port-80 redirect reflected the client-controlled `req.headers.host` into its `Location`, an open-redirect and cache-poisoning vector where a cache sits in front of `:80`; it now redirects to a fixed canonical host from the environment. The rate-limit recipe recommended express-rate-limit v7, which allowed an IPv6 client to bypass a per-IP login limit by rotating addresses within its ISP-assigned subnet; the orchestrator source-verified against the v8.0.0 changelog (claude could not reach that host) that v8 masks each IPv6 client to a `/56` subnet by default and closes that bypass, so the guide now recommends v8 or newer and notes an IP limit alone does not stop credential-stuffing from many addresses (pair it with per-account controls). The password-hashing lead framed bcrypt as primary with argon2 an "equivalent alternative", contradicting `authentication.md` rule 4 ("prefer argon2id; ... bcrypt only for legacy systems"); it now prefers `argon2id` for new applications and keeps the bcrypt example for existing systems. The client-TLS rule said both `NODE_TLS_REJECT_UNAUTHORIZED=0` and `rejectUnauthorized: false` "disable certificate validation for every connection"; corrected to distinguish the process-wide environment variable from the per-connection option (which also affects any connection sharing an agent configured with it). As an entailed cross-guide fix, `nginx.md`'s two `location /` blocks set `Host`, `X-Forwarded-For`, and `X-Forwarded-Proto` but not `X-Forwarded-Host`, which nginx forwards from the client by default (`proxy_pass_request_headers on`, source-verified) and which Express derives `req.hostname` from under `trust proxy` (the behind-proxies doc, claude LIVE-VERIFIED, requires the last trusted proxy to overwrite `X-Forwarded-For`, `X-Forwarded-Host`, and `X-Forwarded-Proto`); both blocks now overwrite it with `$host`. Both families confirmed correct and unchanged: the `trust proxy` semantics and the "never `true`" and hop-count cautions (claude LIVE-VERIFIED against expressjs.com behind-proxies), every `express-session` option and the MemoryStore/no-default-maxAge notes (claude LIVE-VERIFIED against the session middleware doc), the `helmet()` HSTS default (codex verified `max-age=31536000; includeSubDomains`), `https.createServer(...).listen(443)`, the `bcrypt.hash`/`bcrypt.compare`, `crypto.timingSafeEqual`, otplib, and qrcode APIs, the `limit` rate-limit option name (current, `max` still supported), and all internal cross-links resolve. (Two codex-only low items were assessed and dropped as non-defects: the `NODE_EXTRA_CA_CERTS` setuid/file-capability caveat could not be live-verified this session and the current text is not wrong, and the `CAP_NET_BIND_SERVICE` privileged-port statement holds at the default `ip_unprivileged_port_start` threshold.)
- Hardened `python.md` against a two-family, source-verified audit (#171; codex gpt-6-astra full-source and claude-opus-4-8 with partial network access both returned DO NOT SHIP). The `SECURE_PROXY_SSL_HEADER` caveat only said the proxy must "set" the header, but Django requires the proxy to STRIP any client-supplied `X-Forwarded-Proto` from every request and set its own from the real connection scheme; otherwise a client can spoof it and make Django treat plain HTTP as HTTPS. The `0.0.0.0`-bind rule ("only when the process itself terminates TLS") wrongly branded the mandated managed-platform configuration as misconfigured and contradicted the sibling `paas.md`: on Render, Fly, Railway, and Heroku the app must bind the platform-required address on the injected port (0.0.0.0, or :: on Heroku Fir) while the platform terminates TLS at its ingress (a finding pre-flagged during the paas.md #166 audit and confirmed by both families against the platform docs), so the rule now carves out platform-terminated ingress, the intro and the `ss` Verify note reference it, and the `$PORT` premise is refined (Fly matches its `internal_port`). The Django HSTS example set `SECURE_HSTS_INCLUDE_SUBDOMAINS = True` with a one-year `max-age`, committing every subdomain to HTTPS-only before they are confirmed HTTPS-ready and irreversibly for the duration; it now starts with a small `max-age` and `includeSubDomains` off, to be raised after HTTPS is validated (the same footgun fixed in apache.md #168). The Verify authentication probe (`curl -s https://example.com/api # expect 401/403`) printed the body not the status, did not disable client proxies, used a generic path, and had no positive control; it now disables client proxies with `--noproxy '*'`, shows the status, targets a real protected path, requires a rejection without credentials and acceptance with a valid one (noting that a reverse proxy in front can enforce it too, so the layer is confirmed by probing the app directly or in its logs), and adds a valid-credential positive control read from a file. The claim that the `fastapi.security` classes "validate nothing" is corrected: they check the credential's presence and scheme or format but do not authenticate it (the `OpenIdConnect`-stub warning stays). Both families confirmed correct: the Flask/Werkzeug `adhoc` dev-only note, the Gunicorn and Uvicorn TLS flags, the Django setting names and `check --deploy`, the argon2 API, `secrets.token_urlsafe`, the `REQUESTS_CA_BUNDLE`/`SSL_CERT_FILE` client-CA variables (including httpx honoring `SSL_CERT_FILE`), and the `http.server` all-interfaces/symlink/not-for-production warning; codex verified every external link resolves.
- Hardened `object-storage.md` against a two-family, source-verified audit (#170; codex gpt-6-astra had full access to all five providers' docs and returned DO NOT SHIP, while claude-opus-4-8 could reach only the AWS and Azure hosts and returned SHIP, so codex is the authority for the R2, GCS, and Supabase claims). The IAM Access Analyzer note over-promised: it lists public buckets only in a Region that has an account-level external-access analyzer, its findings update asynchronously, and archived findings drop from the active view, so an empty list is not proof; the guide now says to create an analyzer per bucket Region, review archived findings, and confirm Block Public Access directly. The Verify anonymous probe now runs the authorized signed GET first as a positive control (S3 returns 403 for an object you cannot list, so an isolated anonymous 403 could just mean the object is missing), disables client proxies with `--noproxy '*'` on every probe, notes that one object does not establish bucket-wide privacy, and treats a DNS/TLS/proxy error as inconclusive. The `gcloud storage sign-url` note is corrected: signing needs a service account (via `--impersonate-service-account` or an activated key), not ordinary `gcloud auth login` user credentials, with the 12-hour limit for system-managed signing and 7 days for a `--private-key-file`. The GCS public-access-prevention Verify now accepts `inherited` when a `storage.publicAccessPrevention` org policy enforces it (it previously demanded `enforced`, contradicting the guide's own line about org/folder/project enforcement). The R2 token note now distinguishes `Admin Read & Write` (can create and delete buckets) from `Admin Read only` (cannot), and the Azure anonymous-denial code is clarified as `401` for modern clients and `409` for legacy clients predating the bearer challenge. Both families confirmed correct: the S3 Object Ownership default and Block Public Access settings, the presign default/maximum, the R2 defaults and SigV4 limits, the GCS uniform-access 90-day irreversibility and PAP `412`, the gcloud update boolean flags, the Azure account defaults and SAS guidance, and the Supabase private-by-default RLS and the `owner_id` policy column.
- Hardened `admin-uis.md` against a two-family, source-verified audit (#169; codex gpt-6-astra full-source found a HIGH plus three MEDIUM, and claude-opus-4-8, network-blocked, independently confirmed the HIGH against the corpus's own precedent). The Verify HTTP check (`curl -sI ... # 401/403 or a login redirect, never a dashboard`) could not establish that a dashboard is protected: a HEAD reads no body, so a 200 login page and a 200 dashboard are indistinguishable, a generic redirect could be canonicalization rather than authentication, and client proxies were not disabled (a proxy's own redirect reads as the panel's login gate). This is the same class the corpus ruled DO NOT SHIP on in paas.md #166. The check now disables client proxies (`--noproxy '*'`), makes an unauthenticated request that must draw a denial from whatever protects the panel (its own 401/403, the reverse proxy's, or a Cloudflare Access challenge) or a redirect into that layer's login, reads the body (a login page and a dashboard can both be 200, and an arbitrary redirect is inconclusive), and requests a known protected route (positive-controlled, a 404 or error being inconclusive) to catch SPA panels that return 200 for both the login page and the dashboard. The concrete Grafana and Prometheus recipes bound only TLS and passwords, leaving both on their default all-interface listeners despite the guide's own loopback rule; Grafana now sets `http_addr = 127.0.0.1` and Prometheus starts with `--web.listen-address=127.0.0.1:9090`. The mongo-express enable-variable claim was corrected: it is not a clean "1.0.x reads `ME_CONFIG_BASICAUTH`, 1.1.0 reads `ME_CONFIG_BASICAUTH_ENABLED`" boundary (codex source-verified that v1.0.0 keys the login off a non-empty username and that `ME_CONFIG_BASICAUTH_ENABLED` already exists in v1.1.0-rc-4), so the guide now says to set both flags, use non-empty credentials, and verify the login actually appears rather than assert the boundary. The `ss` note now accounts for Docker-published and NAT-only ports and points to the external probe as authoritative. Both families confirmed correct: the mongo-express variable names and `admin`/`pass` defaults, the Grafana first-login and HTTPS keys and anonymous-off default, the Prometheus `basic_auth_users`/`tls_server_config`/`promtool` config, the bcrypt-cost comment (unchanged from #163; `$2y$` hashes validate against a `$2b$` placeholder), and the Adminer and phpMyAdmin/pgAdmin advice. (Out of scope, recorded for follow-up: `bi-dashboards.md:49` carries the same unguarded `-sI ... never a dashboard` probe, and a `run_all_checks.sh` gate could flag external panel/app Verify curls that lack `--noproxy '*'` or an "any other 3xx is not a pass" qualifier.)
- Hardened `apache.md` against a two-family, source-verified audit (#168; codex gpt-6-astra full-source found six MEDIUM issues, and claude-opus-4-8, after several OOM retries on the shared host, completed and concurred on the ship-blocking mutual-TLS finding). The mutual-TLS block carried no placement guidance, so a reader adding `SSLVerifyClient require` to the human-facing vhost would reject every browser user during the TLS handshake before basic auth applied; the guide now says to put mTLS in a separate machine-client vhost and keeps `SSLCACertificateFile` at server/vhost level. The `SSLProtocol` floor is set inside the vhost, but a per-vhost `SSLProtocol` takes effect independently only on Apache 2.4.42+ (with OpenSSL 1.1.1+ and SNI); on older builds the base or first-listed vhost decides, so the guide now says to set the same floor in the base config. The `certbot --apache` note no longer claims it performs steps 1 to 3 (it installs the certificate and usually the redirect, but `--hsts` is off by default, which also reconciles the guide's own HSTS warning). The HSTS example drops `includeSubDomains` from the default and says to add it only once every subdomain serves valid HTTPS, because it forces them all onto HTTPS and blocks any HTTP-only one. The htpasswd path is annotated as Debian/Ubuntu with the RHEL/Fedora `/etc/httpd/conf/.htpasswd` equivalent (the RHEL note on the `AuthUserFile` line is placed on its own comment line, since Apache config has no inline comments). The three Verify HTTP probes gain `--noproxy '*'` and `-sS` so a client proxy cannot answer for the server and transport errors stay visible. codex confirmed correct: the TLSv1.3 version gate, the `SSLCertificateChainFile`-deprecated-since-2.4.8 claim, the bcrypt cost, the directive names, the package/module commands, the still-current "Authelia does not support Apache" claim, and the mod_auth_openidc OIDC path.
- Hardened `deployment-lifecycle.md` against a two-family, source-verified audit (#167; both codex gpt-6-astra and claude-opus-4-8 returned DO NOT SHIP, agreeing on the stale Vercel hedge). Section 3 carried a hedge that "the configuration reference page itself still listed All Deployments as Pro and Enterprise only", which the live Vercel Deployment Protection reference now contradicts ("Vercel Authentication for All Deployments... do not require a paid add-on") and which disagreed with the sibling `paas.md`; a Hobby-plan reader could have read it as needing a paid upgrade to protect the production domain and left it open, so the hedge is removed and the guide now matches the live docs, with the 9 September 2026 changelog added to Sources. The teardown step now keeps the released resource reserved until the record's old TTL drains and removal is confirmed at the authoritative nameservers, because deleting a record does not flush answers already cached by resolvers (a released IP is still reachable by cached clients during the TTL window). The retired-preview Verify was tightened: a `curl` connection/DNS/TLS failure is not proof the record is gone (a dangling record to a released target fails the same way), so the `dig` check now queries A, AAAA, and CNAME rather than only the default A (and, because a plain `dig` only reflects the configured resolver, the guide says to confirm removal against the authoritative nameservers) (an AAAA-only dangling record was invisible to the old check, contradicting section 1's IPv6 requirement), treating SERVFAIL/timeout as inconclusive. The external `nmap -p-` sweeps are annotated as TCP only, with a pointer to probe UDP services separately (`nmap -sU`). Both families confirmed the rest sound: the safe-initialization order, the setup-wizard re-check, the DNS-before-app teardown order, the offboarding layers, the guarded IPv4/IPv6 nmap probes (including that the IPv6 placeholder is caught), the `nc`/`dig`/`nmap` flags, and the identity-verifying external certificate-expiry `openssl s_client` pipeline.
- Hardened `paas.md` against a two-family, source-verified audit (#166; both codex gpt-6-astra and claude-opus-4-8 returned DO NOT SHIP on the same HIGH Verify gap, with all dated vendor claims confirmed current). The Verify check `curl … /api/… # 401/403 without credentials` did not distinguish the application's own authentication from the platform's deployment-protection gate: Vercel Authentication (and similar gates) returns 401 before the app is reached, so the check passed even when the endpoint had no application auth, and once the reader disables deployment protection to serve the public the endpoint is open, exactly the failure the guide's section 1 warns about. The Verify section now disables client proxies (`--noproxy '*'`) and prints the status like the rest of the corpus, tests the app's own rejection with deployment protection disabled (or a Protection Bypass token) plus a positive control that an authorized identity succeeds, frames the HTTP->HTTPS redirect as a spot-check that does not prove app-wide HTTPS or proxy-awareness, and scans the deployed browser bundle (not just the repository) for every credential type. Section 4 now says managed databases must also be network-restricted (many, including Render's Postgres, are reachable from any IP by default, so TLS alone is not enough) and that certificate verification support varies by endpoint (Render's internal connections do not support `sslmode=verify-full`, so set `sslmode=require` to force encryption there and do not treat it as identity-verified). The Password Protection note now gives the per-plan split (unavailable on Hobby, paid on Pro, included on Enterprise), and the Hugging Face "public by default" line is qualified because the default depends on the creation path and organization policy. Both families confirmed the current vendor facts: Vercel Authentication for All Deployments is free on every plan including Hobby (the 9 September 2026 changelog is live), the HF visibility tiers, the Static-Space `window.huggingface.variables` secret exposure, Dev Mode's SSH/VS Code endpoint, the `NEXT_PUBLIC_` client inlining, the Express/Django proxy settings, and the links. (Out of scope, recorded for python.md's own audit: python.md:31 allows binding `0.0.0.0` only when the process terminates TLS, which needs a platform-ingress exception since PaaS platforms terminate TLS at their edge.)
- Hardened `mongodb.md` against a two-family, source-verified audit (#165; codex gpt-6-astra returned DO NOT SHIP on the Verify section, claude-opus-4-8 returned SHIP with the same items as LOW). The Verify section was entirely negative, so a completely unreachable or misconfigured deployment passed every check and read as "hardened"; it now adds an authenticated positive control (an `admin` connection over TLS must succeed and list databases), reframes the plaintext probe (the signal that matters is a plaintext connection that succeeds, since a bare refusal also occurs when the host is simply unreachable), makes the checks non-interactive with `--eval`, and requires an attributable MongoDB authorization error rather than any failure. The admin-bootstrap sequence now authenticates the shell (`db.getSiblingDB("admin").auth(...)`) after the localhost exception ends and before creating application users, which the exception does not do for you (verified at the MongoDB SCRAM setup procedure). The concatenated server PEM step now sets ownership to the account `mongod` runs as (`mongodb` on Debian/Ubuntu packages, `mongod` on RPM) alongside `chmod 600`, so a reader whose `mongod` cannot read a root-owned key fixes ownership rather than loosening the key to world-readable. The driver note no longer implies every driver accepts `tlsCAFile` in the connection string (the option "is not supported by all drivers"). Both families confirmed the rest correct, including the `net.tls.CAFile` / `allowConnectionsWithoutCertificates` client-certificate semantics the brief flagged (client certs are required by default and `allowConnectionsWithoutCertificates: true` permits SCRAM-over-TLS without one while still validating any presented certificate), the `bindIp` localhost and `authorization` disabled defaults, the SCRAM-SHA-256 default (since 4.0), the localhost exception, `requireTLS`, and the links.
- Hardened `minio.md` against a two-family, source-verified audit (#164; both codex gpt-6-astra with full doc access and claude-opus-4-8 returned DO NOT SHIP on the same HIGH wrong-default). The guide said the S3 API is on "loopback or private networks by default", but MinIO's `--address` defaults to `:9000` on every interface, so a default install is internet-reachable while the guide's own next sentence warns that a host-less bind exposes all interfaces; the guide now states the all-interface default and gives the explicit `--address 127.0.0.1:9000` bind (verified at the AIStor server reference). The console Verify probe hard-coded `:9001`, but an unset `--console-address` picks a random port, so the probe passed while an exposed console sat on the real port; it now probes the actual console port from `ss`/the startup log, treats any response as reachable, and states a refusal or TLS error is not proof of isolation. The anonymous-access checks proved denial only for the tested GET, so the guide now inspects each bucket's full policy with `mc anonymous get-json` (which shows the upload and prefix grants that the GET probes cannot audit). The `mc alias set` note is corrected to say it performs a signed probe that does not by itself prove the keys are valid (a fronting proxy's 403 can satisfy it), so the app's required operations must be tested against MinIO's own endpoint. The `minioadmin`/`minioadmin` warning notes it is still the built-in default when the root environment variables are unset, and the console-source citation now points at `cmd/common-main.go`, where the host-less binding is implemented. Both families confirmed the rest correct: the lifecycle claim (the community repo was archived 2026-04-25 and is unmaintained, with AIStor Free and AIStor Enterprise succeeding it), the `MINIO_ROOT_USER`/`MINIO_ROOT_PASSWORD` names, the `public.crt`/`private.key` auto-HTTPS in `~/.minio/certs`, the console flag semantics, and the links.
- Swept the corpus for basic-auth password hashes generated at bcrypt cost 5 and raised them to cost 12 (#163). `apache.md` and `nginx.md` created their htpasswd files with bare `htpasswd -B` (bcrypt default cost 5, below the OWASP minimum of 10 that authentication.md sets), and `admin-uis.md`'s comment and `devops-uis.md`'s registry-auth prose showed the same bare `-B`; all now specify `-C 12`, matching the fixes already made to traefik.md, caddy.md, and ollama.md. `secrets.md`'s `htpasswd -cbs` example is deliberately left: it uses the `-b` argv flag as an illustration of a secret exposed on the command line, not a bcrypt-cost recommendation.
- Hardened `ollama.md` against a two-family, source-verified audit (#162; codex gpt-6-astra with full doc access returned DO NOT SHIP with six findings, claude-opus-4-8 was network-blocked and flagged the ship-blocking hash issue offline against the repo's own standard). The nginx `auth_basic` example generated its hash with bare `htpasswd -B` (bcrypt cost 5, below the OWASP minimum of 10 that authentication.md sets); it now specifies `htpasswd -B -C 12`, matching traefik.md and caddy.md. The Caddy example's `reverse_proxy` and the Cloudflare Tunnel route did not override the upstream `Host`, so Ollama's loopback host check would reject the authenticated request with 403; the Caddy block now sets `header_up Host localhost:11434` and the tunnel note sets `originRequest.httpHostHeader` (the nginx block already did the equivalent). The Docker loopback-publish advice (`-p 127.0.0.1:11434:11434`) now notes that a localhost-published port is only isolated on Docker Engine 28.0 and later; before 28.0 hosts on the same layer-2 segment can still reach it (verified at the Docker port-publishing docs). The proxy examples authenticated but did not rate-limit, so the guide now requires attempt throttling plus concurrency and request-time caps before publication and names the denial-of-wallet risk that authentication.md rule 9 calls out for exposed inference endpoints. The bearer-token `if` variant is now marked a convenience whose `!=` is not constant-time and whose token lives in the config, pointing readers to `auth_basic` or an Access service token. The Verify section now prints the HTTP status (rather than only the body), adds a wrong-credentials negative control (expect 401, never 200), and states that an absent host listener under Docker is inconclusive because a NAT-published port need not appear in `ss`. Both families confirmed the correct claims: the `OLLAMA_HOST` loopback default and the `0.0.0.0` exposure, the Docker image's `ENV OLLAMA_HOST=0.0.0.0:11434`, bare bridge `-p` publishing to all host addresses, `--network host` discarding `-p`, the guarded external probe's refusal to run on an unsubstituted placeholder, that a 401 on the unauthenticated `/api/tags` comes from the proxy (that endpoint does not authenticate callers in Ollama), the `basic_auth` Caddy directive name, and the curl write-out version.
- Hardened `mysql.md` against a two-family, source-verified audit (#161; codex gpt-6-astra with full MySQL and MariaDB doc access returned DO NOT SHIP, claude-opus-4-8 returned SHIP with one source-verified LOW). The Verify section confirmed the server configuration and per-account `REQUIRE` settings but never exercised the guide's core control end to end, so it now adds a client positive control (an `--ssl-mode=VERIFY_IDENTITY` connection to the deployment hostname must succeed) and a negative control (an `--ssl-mode=DISABLED` cleartext attempt must be rejected, not time out or wait at a password prompt: the `REQUIRE SSL` account rejects it with ERROR 1045, evaluated before the global check, and `require_secure_transport` independently refuses cleartext for any account with ERROR 3159), and the `\s` note now says it reports this session's cipher only, not server-wide enforcement or identity. Because MySQL's auto-generated server certificate does not carry the deployment hostname, the guide now states that the `VERIFY_IDENTITY` client needs a certificate covering the hostname clients use (verified at the MySQL client-options and automatic-certificate-generation docs). The X Protocol default-on exposure is no longer framed as 8.4-only: the X Plugin is enabled by default with `mysqlx_bind_address` = `*` on MySQL 8.0 as well (verified at the 8.0 X Plugin options doc). Added a conditional note that on MariaDB using systemd socket activation the TCP listener is defined by the `mariadb.socket` unit rather than `bind_address`. Both families confirmed the defaults and version claims correct: the `require_secure_transport` semantics (Unix-socket connections exempt), the `caching_sha2_password` default, the multi-factor FIDO/WebAuthn chronology and Enterprise-only server plugins, and the MariaDB divergences (no X Protocol, `require_secure_transport` since 10.5.2, the client's `--ssl-verify-server-cert`).
- Hardened `redis.md` against a two-family, source-verified audit (#160; both codex gpt-6-astra and claude-opus-4-8 flagged the protected-mode gap, and codex additionally found the ACL and version issues with deep source inspection). The least-privilege ACL example (`~app:* +@read +@write`) was not least-privilege: key patterns do not constrain commands that take no key argument, so `+@write` still permitted keyless commands like `FLUSHALL` (clears every database), `FLUSHDB` (the current database), `SWAPDB`, and the Redis 7 `FUNCTION` write subcommands (`LOAD`/`DELETE`/`FLUSH`/`RESTORE`), none constrained by `~app:*`; the guide now removes the destructive flushes explicitly and, because command categories still grant keyless and keyspace-discovery commands (`KEYS`/`SCAN`/`RANDOMKEY` in `@read`), points readers to a per-command allowlist (`~app:* +get +set`) for real least privilege (verified at the Redis ACL and command docs). The `protected-mode` description omitted that on Redis 6.x it only restricts to loopback when there is no explicit `bind` as well as no password, so an explicit `bind 0.0.0.0` with no password is fully exposed (7.0 drops the no-bind condition); the text now states both preconditions. The ACL alternative now makes securing the `nopass` `default` user (`user default off resetpass` or a password) a required step before remote exposure rather than a deferred one, and notes already-authenticated connections persist. The `bind 127.0.0.1 -::1` example is annotated that the `-` optional-address prefix requires Redis 6.2 (6.0 needs plain `bind 127.0.0.1`). The TLS block notes that `tls-auth-clients` defaults to `yes` and the guide's `no` is a deliberate downgrade, and the client/Verify commands now supply `--cert`/`--key` for the mTLS path and no longer imply `NOAUTH` without them. The plaintext Verify probe now treats any RESP reply as proof the port is open and a DNS/timeout failure as inconclusive rather than a pass. Both families confirmed the directives, the empty `requirepass` default, the PING-not-in-@read/@write claim, and the TLS/port-0 config as correct.
- Hardened `traefik.md` against a two-family, source-verified audit (#159; codex gpt-6-astra with full doc access found the issues, claude-opus-4-8 was network-blocked and SHIPped on recall). The basicAuth example used `htpasswd -nB` (bcrypt cost 5, below the OWASP minimum of 10 that authentication.md sets); it now uses `-nB -C 12`, and both illustrative hash prefixes move to cost 12. The middleware order put `app-auth` first, so basicAuth's 401 short-circuited before the rate and in-flight limiters, leaving failed-login attempts unbounded (contradicting authentication.md's rate-limit-auth rule); the order is now `app-rate,app-inflight,app-auth,app-body`, with the rationale and the Verify comment updated to match. The Verify section no longer treats a `401` as proof that Traefik's basicAuth is attached (the app's own login can also return 401): it confirms routing, and the guide now points to the router's `middlewares` for component attribution. The file-provider basicAuth alternative now says to enable it with `providers.file.directory` and to reference a file-defined middleware from a Docker router as `app-auth@file`. The unlabelled-canary checks carry an explicit "reasoned from documentation, not demonstrated against a live deployment here" qualifier. Both families confirmed the defaults (`exposedByDefault=true`, `api.insecure=false`, the ACME production CA, the v3 Swarm-provider split) and that the guide has no `insecureSkipVerify`, staging-CA, forgeable-forwardAuth-header, or untrusted-XFF defect; the legacy middleware doc URLs redirect to the current references (not broken).
- Hardened `firebase-supabase.md` against a two-family, source-verified audit (#158; codex gpt-6-astra found the issues, claude-opus-4-8 SHIPped). Two HIGH fixes: the PostgreSQL 14 view fallback revoked only `select`, but a simple view is automatically updatable and its DML runs with the owner's privileges, so `insert`/`update`/`delete` could still write through it and bypass the table's RLS while the SELECT-only check passed; it now revokes ALL privileges and confirms every privilege is gone (verified at the PostgreSQL 14 CREATE VIEW docs). And "New projects start in locked mode" was corrected: creating a Firestore database offers a test mode that lets anyone read and overwrite the data (about 30 days), and Storage/RTDB have their own starting rules, so the guide now tells readers to inspect the deployed rules per instance rather than assume a locked default (verified at the Firestore quickstart). The bundle leak scan switches to `grep -rlIE` (filenames only, so a key is never printed to a shared log; inspect flagged files privately), matching secrets.md and Supabase's "don't log a key." The Verify section seeds known A/B-owned fixtures and requires an authorized owner request to succeed, so an empty RLS-filtered result is not mistaken for enforcement, and it now tests cross-user writes too. Noted that a no-RLS table is reachable by `anon` through its default grants (grants and RLS are separate checks). Both families confirmed the key-name (anon/service_role and the 2025 publishable/secret rename), RLS-default, `security_invoker`, `aal2`-policy, and SECURITY DEFINER claims as correct.
- Tightened Verify discrimination and a broker-access contradiction in `host.md` against a two-family audit (#157; codex gpt-6-astra found the Verify gaps, claude-opus-4-8 SHIPped with a version-attribution nit, both source-verified). The firewall checks no longer accept an unrestricted SSH allowance: removing the firewalld `ssh` service leaves a separate `22/tcp` or broad source rule open, and a broad ufw "from <range> to any" allow admits SSH without a port-22 rule, so the guide now has readers check every effective allowance (ports, source rules, zone target). The brokered-access line is corrected to match cloud-firewalls.md: only agentless SSM and a tailnet need no inbound rule, while GCP IAP (`35.235.240.0/20`) and Azure Bastion (`AzureBastionSubnet`) need a scoped allow. The listener census becomes `ss -tulnp` (TCP and UDP); the external `nc` probe now says a refusal is not proof the host firewall denied it (a host with no sshd refuses too); the password check requires that no password prompt is offered (a keys-only server denies bad credentials too); and the MFA check requires rejecting an omitted/invalid factor and closing the `nullok`/`failmode=safe` bypasses, not merely seeing a prompt. The key-access prerequisite now confirms a non-root sudo-capable admin before root login is disabled, and the OpenSSH `Include` note is corrected (the directive dates to 7.3; the default drop-in became standard around 8.2). Both families confirmed the SSH directives, defaults, drop-in precedence, and firewall syntax as correct.
- Hardened `postgresql.md` against a two-family, source-verified audit (#156; both codex gpt-6-astra and claude-opus-4-8 verified the defaults and `pg_hba.conf`/`sslmode` semantics as correct, and codex found the Verify-discrimination gaps). The load-bearing fix closes a TLS-verification bypass: libpq prefers GSS encryption over TLS regardless of `sslmode` where a Kerberos/GSSAPI environment exists, so a `verify-full` connection could be satisfied by GSS and skip the certificate check; every `sslmode=verify-full` example and Verify probe now sets `gssencmode=disable` (verified at the libpq connection-parameters docs). The Verify section gains a runnable plaintext probe (`sslmode=disable gssencmode=disable`) that must be refused by `pg_hba.conf` rather than time out, a SCRAM-verifier check (`pg_authid.rolpassword LIKE 'SCRAM-SHA-256%'`) because the wrong-password negative control passes for a legacy md5 account too, and `pg_stat_ssl` is relabeled session diagnostics rather than an enforcement test. Added a reload step after editing `pg_hba.conf` (the numbered steps otherwise left the old policy active on Unix), and corrected the note that pre-existing passwords are necessarily md5 (each keeps whatever format it had when set; verified at the password-authentication docs). Added the libpq connection-parameters and password-authentication sources.
- Hardened `machine-auth.md` against a two-family, source-verified audit (#155; codex gpt-6-astra, which had full source access, found the issues claude-opus-4-8 could not verify because its sandbox blocked the GitHub/GCP/RFC hosts). The GCP Workload Identity Federation recipe restricted only the org and repository, not the branch or environment that section 4 tells readers to pin, so it now conditions on `assertion.ref` too (verified against Google's deployment-pipeline federation example). Access-token validation no longer points readers at the ID-token recipe in oidc-integration.md: it now gives the RFC 9068 access-token rules (signature, exact issuer, an `aud` naming your API, expiry, and the `at+jwt` type so an ID token cannot be substituted). Actions are pinned to a full-length commit SHA rather than a mutable release tag; the client-credentials refresh-token note is softened to RFC 6749's "SHOULD NOT"; the Verify probe reads the bearer key from a mode-0600 header file instead of the command line (its own section 1 rule, verified via curl `-H @file`), adds a positive control, closes a trailing-wildcard hole in the federation-subject check (`...:example-repo:*` had passed while admitting every branch), switches the image scan to `docker history --no-trunc` with a real placeholder and a layers-scan caveat, and adds an mTLS accept/reject check. Added RFC 9068 to Sources. The guide's strong core (federation over static keys, constant-time compare, at-rest hashing, no keys in URLs, mTLS revoke-and-test) was confirmed by both families.
- Closed Verify-completeness gaps in `cloud-firewalls.md` against a two-family, source-verified audit (#154; codex gpt-6-astra + claude-opus-4-8). Two of the three cloud enumerations could report "clear" while a port was world-open: `gcloud compute firewall-rules list` returns only classic VPC rules and misses hierarchical and network firewall policies, which GCP evaluates first (both families flagged this; verified at the GCP firewall eval-order docs), and `az network nsg rule list` misses Azure Virtual Network Manager security admin rules, whose "Always allow" bypasses the NSG entirely (verified at the Azure NSG overview). The guide now enumerates the GCP policy layers and reviews Azure security admin rules. Corrected the SSH-broker guidance: only AWS SSM Session Manager is agentless (no inbound rule); GCP IAP needs an ingress allow for TCP 22 from `35.235.240.0/20` and Azure Bastion needs the VM to allow the `AzureBastionSubnet` (verified at the IAP TCP-forwarding docs), so "remove the inbound rule entirely" was wrong for those, and the Verify's "named address, never an open range" now admits the documented broker ranges while still forbidding `0.0.0.0/0`. Clarified that the `nc` port list is a non-exhaustive spot-check whose refusal does not prove a cloud-firewall deny (an allowed port with no listener refuses too), that RDP and IPv6 need covering, and that the full-range external scan from a disallowed source is the authoritative test. Both families confirmed the guide's careful choices elsewhere (no stateful/NACL-default traps, AWS new-SG "no inbound rules", SG-focus sufficiency, the full-IPv4 anti-evasion note).
- Hardened `docker.md` against a two-family, source-verified audit (#153; codex gpt-6-astra found the HIGH, claude-opus-4-8 concurred on the `ss` gap; claude's WebFetch was blocked so its externals were NOT-LIVE-VERIFIED). The firewall section had a real IPv6 gap: publishing a port without a host IP on an IPv4-only bridge also exposes it on the host's IPv6 addresses through Docker's userland proxy (IPv6 mapped to the container's IPv4, verified at the Docker port-publishing docs), and that path terminates on a host process so it traverses `INPUT`, not the `FORWARD`-chain `DOCKER-USER` rule the guide told readers to add; the guide now closes it (publish to a specific address, disable the userland proxy, or restrict the host IPv6 `INPUT`) and probes IPv4/IPv6 separately. The Verify section is tightened: `docker compose ps` is labeled project-scoped (it misses containers outside the project and specific-address/IPv6 bindings), `ss -tlnp` becomes `ss -tulnp` with a note that a DNAT-published port with the userland proxy disabled shows no listening socket at all (both families flagged this), and the authentication probe now prints `http_code` instead of an empty body that made an exposed `200` and a protected `401` indistinguishable. Added a cross-link to `container-hardening.md` for capability-drop, `no-new-privileges`, read-only-rootfs, and socket-mount hardening. Both families agreed the guide makes no unsafe statement (no socket mount, no `--privileged`, no container-as-boundary claim); the Docker 28.0 and `DOCKER-USER`-iptables-only claims were left as-is (accepted by both families, DOCKER-USER placement verified).
- Hardened `oidc-integration.md` against a two-family, source-verified audit (#152; codex gpt-6-astra + claude-opus-4-8, reconciled). ID-token validation now rejects a token carrying an untrusted additional audience and checks `azp` when `aud` is multi-valued (OIDC Core 3.1.3.7 requires rejecting "additional audiences not trusted by the Client"; it had only required `aud` to contain the client ID). The authorization-code recipe now sends the `code_verifier` at the token exchange, the Python Authlib example enables PKCE (`code_challenge_method: 'S256'`) the guide had promised, and the Node openid-client example adds the missing `nonce`/`expectedNonce` and stores state/verifier/nonce in the server-side session. Microsoft Entra issuer validation is corrected: `common`/`organizations` return a templated issuer `https://login.microsoftonline.com/{tenantid}/v2.0`, so the guide now has you substitute the token `tid`, match `iss` exactly, restrict the signing key by its issuer scope, and enforce a tenant allowlist rather than expecting the issuer to equal the fetched prefix. The Verify section stops replaying an ID token "at the callback" (which only ever receives a code) and instead exercises the section-2 validator with expired/wrong-`aud`/bad-signature fixtures, and the `/admin` probe inspects the redirect target. Logout guidance is aligned to the authentication.md baseline (a copied pre-logout cookie must stop working). Added the log/`Referrer-Policy` leak-path caveat and a refresh-token note, and broadened the GitHub legacy-wildcard warning to subdomains as well as subpaths. Source-verification confirmed GitHub OAuth DOES support PKCE with `S256` (a stale-knowledge flag from one family was refuted at the GitHub docs) and that RFC 9700 says clients SHOULD NOT (not MUST NOT) use the implicit grant (the guide's existing wording was already correct).
- Tightened Verify discrimination and an identity-header caveat in `tailscale.md` (#151; codex gpt-6-astra audit, claude family memory-blocked). The direct-exposure probe used an unguarded literal LAN/VPC address with a `time_connect=0` pass signal (the #100/#106 false-pass shape: an unsubstituted address fails DNS to the same zero, indistinguishable from a filtered port) and honored proxy variables; it now uses the corpus's guarded `set -- PASTE_WHOLE_BLOCK` idiom with `--noproxy '*'` and `exitcode`/`errormsg`, run from another machine. The tailnet Verify checked only that an allowed device connects, which holds under both the default allow-all policy and a scoped one, so it could not detect a failure to remove allow-all; it now says so and directs asserting the restriction in the policy file's `tests` (accept the intended identity, deny an excluded one; Tailscale rejects a policy whose tests fail). The identity-header note no longer treats a loopback bind as proof of Serve provenance: loopback stops other machines but any local process can reach `127.0.0.1:3000` and forge the headers, so trusting them requires trusting every process on the host (Tailscale documents that localhost binding limits, not eliminates, tampering by other local services). Added the Serve identity-headers and policy-tests sources.
- Hardened `self-signed.md` against a source-verified audit (#150; codex gpt-6-astra, which generated the certs and tested with OpenSSL 3.5.5; the claude family stayed memory-blocked). Replaced the deprecated `-nodes` with `-noenc` in the RSA command and prose (deprecated since OpenSSL 3.0, which this guide targets; `-nodes` still parses). The Verify now inspects `basicConstraints` alongside `subjectAltName` and tells the reader to read the output, not the exit status (the inspection exits 0 even for a certificate with no SAN, and `-verify_hostname` falls back to the Common Name when no SAN is present, so a CN-only or `CA:TRUE` certificate would otherwise pass) and to check IP identities with `-verify_ip`. It also distinguishes the `s_client -CAfile` diagnostic (explicit trust) from confirming the section-4 trust-store install, which must be tested from each client's normal configuration (a fresh certificate should fail before `update-ca-certificates` and succeed after). Flagged the example `203.0.113.10` as an RFC 5737 documentation address to replace. All verified at the OpenSSL `req`/verification-options/`x509` man pages and RFC 5737/9525.
- Hardened `free-certificates.md`, the corpus's most-referenced guide, against a source-verified audit (#149; codex gpt-6-astra, with the claude family's audit OOM-killed by shared-host memory). Renewal reliability: a bare `certbot renew --deploy-hook ...` is a no-op right after issuance and never persists the hook (Certbot saves options only when a certificate is actually renewed), so the guide now installs the reload hook durably (`--deploy-hook` on the initial `certonly`/`run`, a `renewal-hooks/deploy/` script, or `certbot reconfigure` on 2.3.0+) and the Verify checks the renewal timer is active and exercises the hook with `--dry-run --run-deploy-hooks` (plain `--dry-run` does not run deploy hooks), plus independent expiry/renewal-failure monitoring rather than CA reminder emails. Standalone issuance now warns that a host whose web server will own port 80 must move renewal to `--webroot`/the server plugin or pre/post hooks. DNS-plugin credentials are scoped to least privilege (a Cloudflare API token with `Zone:DNS:Edit` on only the needed zones, not the Global API Key) and `chmod 600` in a `700` directory. The DNS-01 correction: it needs only control of the challenge `TXT` record and can certify a host with no public address record or inbound port, so the prerequisites and intro no longer present a public `A` record and inbound connectivity as universal (which had conflicted with traefik.md). The network Verify now probes the reader's own hostname through a guarded placeholder instead of `example.com`, which serves a live page and passed spuriously. All claims verified at the Certbot, certbot-dns-cloudflare, and Let's Encrypt docs; the 90-day/6-day lifetimes, the 2027/2028 reductions, the 47-day 2029 CA/Browser maximum, and the OCSP retirement were re-confirmed current and left unchanged.
- Corrected drifted tiers and a fail-open default in `identity-providers.md` (#148), from a cross-family audit (codex gpt-6-astra; the claude family's audit and QA workers were OOM-killed by shared-host memory pressure) with every changed figure source-verified at the vendor. The Duo Authentication Proxy bullet now names its fail-open default (`failmode=safe` admits a primary-authenticated user when Duo is unreachable; set `failmode=secure` for mandatory MFA). Firebase's free allowance is split correctly: the 50,000 MAU (50 SAML/OIDC MAU) figure is the pay-as-you-go Blaze free tier, while the no-billing Spark plan is capped at 3,000 daily active users (2 for SAML/OIDC). Auth0's MFA boundary is sharpened (factors start at the paid Essentials plan; Adaptive MFA needs an Enterprise plan plus its add-on) and the passkey note corrected (a passkey with verified user verification is itself multifactor; what Free lacks is Auth0's licensed MFA policy). Cognito's grandfathered Lite 50,000 MAU free tier (pools created on or before 22 November 2024) is noted. The Verify section gains positive controls (the admin function must succeed after MFA; an allowed account must reach the app) and its logging check is fixed to require the sign-in log plus the app's authorization decision rather than mere log presence, naming Entra Sign-in logs (not Audit logs). Entra External ID (50,000 MAU) and Clerk (50,000 MRU, Pro $25) were spot-verified current and left unchanged. Added four vendor sources.
- Hardened `secrets.md` against a cross-family-reconciled, source-verified audit (#147). The Verify grep printed matching lines (`grep -rn`), so a detected secret was echoed into the terminal or CI log, contradicting the guide's own no-secrets-in-logs rule; it now uses `grep -rlIE` (filenames only, never the secret, and `-I` skips binaries) with `--exclude-dir` instead of a `| grep -v node_modules` filter that suppressed any line merely containing `node_modules` (a false negative that could certify an exposed tree clean), and the pass condition is rewritten to be exit-aware and honest that a prefix grep is a supplementary search, not proof, with gitleaks as the primary gate. The `.gitignore` guidance gained `.env.*`: ignoring `.env` alone leaves `.env.local` and `.env.production` (the files Next.js and similar toolchains load, verified at nextjs.org) committable, and the grep's old `--include="*.env"` could not match them either. Added a `0600` / `umask 077` note for the `~/.pgpass` and `curl --netrc` credential files (PostgreSQL ignores a `~/.pgpass` that group or others can read, and curl warns the `.netrc` must not be readable by others; verified at postgresql.org and everything.curl.dev), a `set -x` caveat (shell tracing echoes an expanded `printf`-into-`--password-stdin` even though the argument list is protected), and a gitleaks version note (the `git`/`dir` subcommands need 8.19 or newer; older builds use `gitleaks detect`; verified at the gitleaks README). Both families were concordant on the `.env.*` gap; source-verification confirmed the PostgreSQL, curl, Next.js, and gitleaks-version claims and added the three new vendor sources.

## 2026-09-16
- Hardened `mfa.md` against a cross-family-reconciled, source-verified audit (#146). Both families (codex + claude-opus-4-8) flagged the same two core gaps: the Verify success signal was tied to enrolment rather than enforcement (rule 6 already taught the distinction, so the check contradicted the guide), and push was recommended with no MFA-fatigue caveat. The password-only Verify now fails only once the factor is enrolled AND enforcement is on (provider policy, Supabase `aal2`, or the app's own check), and states that a still-succeeding password-only session means enrolled but not enforced. Rule 3 ranks push below WebAuthn/passkeys and TOTP, notes neither TOTP nor push is phishing-resistant, and requires number matching or verified push (never a bare tap) against prompt-bombing. The SSH section makes the PAM factor mandatory (`AuthenticationMethods publickey,keyboard-interactive` so a key login still runs the challenge, drop `nullok` after rollout, Duo `failmode=secure` since its default `safe` fails open) rather than implying the module alone enforces MFA. Rule 4 requires re-verifying the existing factor before a user disables or replaces it or mints new recovery codes, so a stolen or password-only session cannot strip MFA. The hosted-identity-provider bullet now says enforcement lives in a layer that varies by provider (a tenant or Conditional-Access policy, a per-user requirement, or authorization in your own app and database; Supabase still admits an `aal1` session unless policies require `aal2`) rather than "only when a tenant policy requires the factor". Cloudflare Access now offers its own independent MFA (a TOTP authenticator or a WebAuthn key) as an alternative to IdP-enforced MFA. The passkey guidance requires verifying the user-verification (UV) flag server-side where a passkey stands alone, treats the sign count as advisory (synced passkeys often report 0) rather than a hard clone-block, and warns that printed recovery codes are the weakest, phishable link for an admin on a phishing-resistant key. The database-MFA section is scoped to unattended machine connections and notes some databases offer native interactive MFA for a human login (MySQL 8 supports up to three factors, including WebAuthn, verified at the vendor documentation). The section heading is corrected to "Identity layers and auth proxies (open source)" since oauth2-proxy delegates authentication rather than enrolling TOTP. Source-verification confirmed MySQL native MFA (codex) and refuted a staleness flag on `otplib` (claude): otplib remains actively maintained, so it stays.
- Closed baseline completeness gaps in `authentication.md` (#145), from a cross-family audit (codex + claude-opus-4-8) that both confirmed the normative core correct (RFC 9700, OIDC hygiene, the hashing set, federated-login-is-not-authorization, MFA-at-grant). Added the controls both families flagged as missing from THE auth baseline: session-ID regeneration on login and privilege change (session fixation), with idle/absolute expiry and server-side logout invalidation; a CSRF defense for cookie sessions (SameSite alone is not sufficient); a password policy plus known-breach screening (new rule 17); a secure account-recovery flow (new rule 18); account-enumeration resistance and a note that per-IP limits do not stop distributed credential stuffing; HTTPS across the whole authenticated session rather than only the credential POST (or an equivalent authenticated encrypted transport such as SSH); and PKCE `S256` (never `plain`) with fresh transaction-bound `state`/`nonce`. Corrected the htpasswd example to `-B -C 12` (the bare `-B` default cost is 5, below OWASP's minimum of 10, verified at the Apache docs), reordered the hash preference to OWASP's argon2id > scrypt > bcrypt > PBKDF2, and noted bcrypt's 72-byte truncation. Broadened the Quick checks to probe every non-public path and transport and to require a real secret scanner.
- Added a gate that keeps `ss` listener probes unfiltered (#144): `tools/check_unfiltered_ss.py` flags any guide Verify block that pipes `ss` to `grep` (the filtered-listener form fixed guide-by-guide in #113 and swept corpus-wide in #138 / row 1.74). A filter cannot reveal a listener on an unexpected port, and a port grep also matches an IPv6 address, so the corpus reads the whole table; this gate keeps the anti-pattern from regressing (zero instances at landing). It is scoped to top-level guide bash and console fences with comments stripped, so a prose warning that names the pattern does not trip it; it carries a 14-case self-test and is wired into `run_all_checks.sh` with the standard self-test-plus-gate-result discipline.
- Hardened `caddy.md` against a cross-family-reconciled, source-verified audit (#143). Adds an HSTS header (Caddy sends none on its own, so "the whole TLS setup" had left the first request downgradable) and states that Caddy ignores untrusted `X-Forwarded-*` by default (use `trusted_proxies` behind a CDN). Flags that fronting with Cloudflare Access needs origin lockdown, since Caddy still answers on 443 and a direct-IP client bypasses Access unless ingress is closed and the Access JWT validated (cross-links cloudflare.md). Verify now proves the `@admin` matcher scopes auth (a non-`/admin` path must be 200, not merely `/admin` returning 401). Plus: the ACME `email` no longer promises expiry notices (Let's Encrypt ended them), an on-demand-TLS abuse caution, the `SIGUSR1` reload conditions, admin-socket default-mode (0200) grounding, and `forward_auth` softened to "Caddy 2.5 and later". Source-verification corrected two of fable's flags to non-defects (Caddy ignores untrusted XFF by default; the admin unix socket defaults to 0200 owner-only) and confirmed codex's Cloudflare-Access-origin finding; both families agreed the `ss` check is adequate.
- Hardened `nginx.md` against cross-family-reconciled audit findings (#142). The behind-CDN case defeated section 4's rate limits (keyed on `$binary_remote_addr`, all callers behind a CDN collapse to the edge IP and the client `X-Forwarded-For` is trusted), so a `real_ip` note (`set_real_ip_from`/`real_ip_header`) is added and the direct-edge XFF handling (read the last entry) is spelled out. The Verify section is corrected: the `/` checks are made consistent and auth-aware on one path, the 413 positive control now requires the app's real response rather than merely "not 413" (a 401 or 000 no longer passes silently), a TLS-floor negative test is added, and `--max-time` bounds the probes. Hardening added: `server_tokens off`, an `ssl_session_tickets off` forward-secrecy note, an `includeSubDomains` readiness caution, a `$host` open-redirect note for a `:80` default_server, and a precise restatement that `add_header` inheritance (not `always`) is what drops a server-level HSTS inside a location. Fable's source-verified reconciliation found no HIGH here (it downgraded codex's `ss`-only HIGH: the loopback-bind `ss` check is adequate for this guide's example).
- Closed origin-lockdown and Verify gaps in `cloudflare.md` (#141), found in a cross-family audit and vendor-confirmed. The two non-tunnel topologies left the origin reachable around Cloudflare while reading as protection: for a directly-exposed proxied origin, an Origin CA certificate with Full (strict) authenticates the origin to Cloudflare but does not stop a client that discovers the origin IP, so the guide now requires Authenticated Origin Pulls (the zone setting AND origin-side `ssl_verify_client`, preferring a zone-level certificate over the shared one, which only proves a request came from Cloudflare's network) plus a firewall restricting 80/443 to Cloudflare's IP ranges; for a remote origin, TLS on that hop only encrypts, so the origin must accept connections only from the `cloudflared` host (with `originRequest.caPool` for a self-signed certificate, never `noTLSVerify`). Verify now carries the load-bearing origin-direct probe from another host (guarded rule-6 idiom), sharpens the pass signals (a 302 to the team's `*.cloudflareaccess.com` login; a service-token GET returns app content; the effective inbound policy rather than "no rule added"), and links `Cf-Access-Jwt-Assertion` validation (cloud-identity-proxies.md section 5) as defense in depth. Also: a One-time PIN left enabled alongside an MFA-enforcing IdP is a bypass (remove it from sensitive applications), and `cloudflared service install` needs an explicit `--config` path under sudo.
- Fixed a through-proxy identity-header forgery and Verify gaps in `fronting-auth.md` (#140), found in a cross-family audit and vendor-confirmed. The guide told the reader to trust `X-Auth-Request-User`, but the oauth2-proxy nginx example delivers identity as `X-User`/`X-Email` and the Traefik middleware carried only token headers, so nginx and Traefik forwarded a client-set `X-Auth-Request-User` straight through to the app and an authenticated user could impersonate anyone. The nginx pattern and Verify now name `X-User`/`X-Email` (the header the proxy overwrites), the Traefik `authResponseHeaders` now carries the identity headers and the section requires `--set-xauthrequest`, and the line-146 overclaim is scoped to the direct probe with a new through-proxy Verify step that replays the trusted header with a valid session and confirms the proxy overwrote it. Also binds the auth service so only the proxy reaches it (loopback, or a private container network; oauth2-proxy 4180, Authelia 9091 defaults noted) and probes 3000/4180/9091 from another host, restores the "from another host" vantage, attaches the Traefik middleware to a router, and sharpens the no-session pass signal.
- Corrected source-verified errors and Verify-discrimination gaps in `cloud-identity-proxies.md` (#139), found in a cross-family audit and confirmed at the vendor. Factual: Google IAP does not enforce its IAP-secured Web App User IAM role for Identity Platform (external-customer) identities, so the app must authorize on the verified assertion's claims, and IAP does not support backend buckets, so the "plus Cloud Storage buckets" claim is dropped; Cloudflare Access can enforce independent MFA (TOTP or WebAuthn) without the IdP, so the blanket "none of these proxies adds a second factor" is qualified; the ngrok explicit-allowlist example was inverted (a non-negated `email in [...]` on a `deny` action denies the allowed user and admits everyone else) and now negates it; and Vercel Password Protection is USD 20 per month per Pro project, not the legacy USD 150 package. Verify now probes the platform-URL bypass the guide calls its main risk (the Cloud Run `run.app` URL and the Vercel production domain), names the "from a host outside your cloud network" vantage for the origin probe, and gives a concrete direct-to-app tampered-assertion check. Minor: ALB `exp` and its signer/client claim locations, the forged-header `deny`-returns-401 signal, a Deployment Protection Exception caveat, and the ngrok origin-lockdown reminder.
- Swept the remaining filtered `ss ... | grep <port>` Verify probes to the unfiltered form (#138, row 1.74): 48 probes across 45 guides now run bare `ss -tlnp`/`-ulnp`/`-xlnp` with a "read every listener" comment carrying each guide's bind expectation, extending the #113 ollama/memcached fix corpus-wide. A filter cannot reveal a listener on an unexpected port and `grep ':(80|443)'` also matches an IPv6 address like `[2001:db8:80::1]:9000`, so the unfiltered form is the one apache.md and traefik.md document; the per-guide ports and bind expectations move into the comment unchanged.
- Added a keyboard skip link to `site/index.html` (#137): a visually-hidden "Skip to content" link, revealed on focus, is now the first focusable element, so a keyboard or screen-reader user can bypass the ~90-link sidebar and jump straight to `<main>` (which gains an `id` and `tabindex="-1"`). The CSP style-src hash in `site/_headers` was repinned for the added rule and the stale pin removed, the first live catch of the row-3.15 orphan check. Rotated row 3.15 (CSP orphan-pin policing, shipped #136) from TODO.md to DONE.md.
- Gated orphan CSP pins in `tools/check_csp_hashes.py` (#136, row 3.15): the multi-page CSP-hash gate proved every listed inline block was pinned but ignored a hash left in `script-src` or `style-src` after its block was edited or deleted, so `site/_headers` could accumulate dead allowlist entries. The gate now records the pins each verified block uses and rejects any hash in a gate-selected directive that no listed block uses; a pin shared across pages is in the used set and is not an orphan, and only the governing directive per kind is policed, matching the gate's existing model. This is a hygiene tightening, not a fail-open fix, and test N17 (which documented the deferral) now asserts the rejection. Rotated row 2.20 (`realtime-voice-infra.md`, shipped #135) from TODO.md to DONE.md in the same change.
- Added `realtime-voice-infra.md`, a guide for self-hosted LiveKit and coturn (#135, row 2.20): AI voice agents run WebRTC media backends at the network edge, and the two most common carry sharp defaults. coturn is a TURN relay, so its stock example configuration (`lt-cred-mech` and `use-auth-secret` both commented out, no users) allocates anonymously, which is an open relay an attacker uses to proxy traffic, reach the host's loopback and RFC 1918 network (which coturn does not block by default), and reflect and amplify UDP; enable a credential mode with a realm, set `allow-loopback-peers=false`, add `denied-peer-ip` ranges for private space, and keep DTLS (opt-in since 4.17.0), the telnet CLI, and web admin off. LiveKit refuses to start with no API keys, so its exposure is a known or weak signing secret rather than an anonymous join: development mode injects a public `devkey`/`secret`, the sample config ships `key1: secret1`, and a sub-32-character secret only logs rather than blocking, and any of them lets an attacker mint a token for any room. Both terminate TLS at a reverse proxy, and STUN Binding stays anonymous regardless of TURN authentication. Verify is reasoned (no container runtime in the authoring environment), tracked by row 2.34.

## 2026-09-15
- Added `self-hosted-error-trackers.md`, a guide for self-hosted Sentry and GlitchTip (#134, row 2.19): an error tracker ingests stack traces, source snippets, request bodies and headers, environment, and user context by design, so an exposed one leaks exactly that (secrets, tokens, PII) through a searchable UI, and the two tools default oppositely on the setting that matters most. Sentry's `getsentry/self-hosted` publishes its bundled nginx on `SENTRY_BIND`, which ships as the bare port `9000` (so it binds every interface), and defaults `auth.allow-registration` to `False`; bind it to loopback behind a proxy, create the first admin with `createuser --superuser`, generate `system.secret-key`, and enforce member MFA and data scrubbing through the organization API. GlitchTip runs on `8000` and defaults `ENABLE_USER_REGISTRATION` to `True`, so an exposed instance offers open self-signup until it is set `False`; bind it private, set `SECRET_KEY`, and enable its MFA. Both expect a reverse proxy for TLS, both keep their backing stores (PostgreSQL, Redis, Kafka, ClickHouse) internal, and both use a client DSN that is a public key by design rather than a secret. Verify ships reasoned (no container runtime) with a registration discriminator and an `ss` inventory; row 2.33 tracks demonstrating it. Extended the `exposure-index.md` 9000 and 8000 rows. Every default verified at the vendor source.
- Added `time-series-metrics-stores.md`, a guide for self-hosted InfluxDB, VictoriaMetrics, and QuestDB (#133, row 2.18): these metrics stores ingest over HTTP and answer queries over an HTTP API with a console beside it, and several authenticate nothing by default or ship a known default credential, so an off-host-reachable instance leaks series, accepts poisoned writes, or allows deletion. InfluxDB 1.x defaults `[http] auth-enabled` to `false` (unauthenticated read, write, and `DROP`), and a fresh 2.x instance is un-set-up so the first caller to reach `/api/v2/setup` becomes its operator; VictoriaMetrics single-node (`8428`) and cluster (`vminsert` `8480`, `vmselect` `8481`, `vmstorage` `8482`) have no built-in request authentication, so the vendor directs you to front them with `vmauth` or a reverse proxy, with `-deleteAuthKey` and its siblings gating only their own destructive handlers; QuestDB leaves the web console and REST on `9000` unauthenticated (Basic-auth keys unset), ships `admin` / `quest` on the PostgreSQL wire port `8812`, and accepts unauthenticated line-protocol writes on `9009`. The guide gives the bind-private-and-front pattern, the exact credential and auth-enable keys, and a reasoned Verify with per-store data-versus-401 discriminators; row 2.32 tracks demonstrating it. Added `exposure-index.md` rows for 8086, 8428, 8480, 8481, 8482, 8812, and 9009, and extended the 9000 row. Every default verified at the vendor documentation.
- Added `transactional-email-posture.md`, a guide for the email-authentication posture of a deployment that sends transactional mail (#132, row 2.13): password resets and invitations are part of the authentication path, so a spoofable sender is a phishing and account-takeover vector, an open relay burns sending reputation, and a plaintext submission leaks the SMTP credential. The corpus had no SMTP or DMARC content at all. The guide is standards-based rather than product-based, with numbered sections for SPF (RFC 7208 terminators, the ten-lookup budget, the multiple-record permerror), DKIM (RFC 6376 with the RFC 8301 crypto floor and selector rotation), DMARC (alignment, the `p`/`sp`/`np` fallback, and the DNS tree walk), DMARC reporting (aggregate `rua` first, failure `ruf` deferred because it can leak a live reset token), SMTP submission and relay (587 STARTTLS versus 465 implicit TLS, STARTTLS stripping, TLS before AUTH, and never an open relay), and transport and display (MTA-STS with its direction caveat, TLS-RPT, BIMI). A material standards correction landed here: DMARC is now RFC 9989 (May 2026), which obsoletes RFC 7489, splits reporting into RFC 9990 and RFC 9991, removes the `pct` tag, and adds `np`; the guide cites the current RFCs and names `pct` only as legacy. Verify ships reasoned (no container runtime, DNS control, or SMTP endpoint) with `dig` and `openssl s_client` inspection and no TLS-skip; row 2.31 tracks demonstrating it. Added `exposure-index.md` rows for 25, 465, and 587. Every record and control verified at the RFC source.
- Added `vault.md`, a deployment guide for self-hosted HashiCorp Vault (#131, row 2.14): the corpus recommends Vault from `machine-auth.md` and `secrets.md` but never covered standing one up. Vault's own config defaults are careful (the listener binds loopback, `tls_disable` is `false`, the UI is off), so the guide is about the deployment posture: never running dev mode (in-memory, no TLS, a printed root token); binding and fronting the `8200` API and `8201` cluster listeners; keeping TLS on the listener; choosing and guarding the seal (Shamir shares versus auto-unseal recovery keys, emitted once, with provider or quorum loss making the ciphertext and backups permanently unreadable); enabling an audit device (off by default, and fail-closed, so Vault refuses to service a request when it cannot write to at least one device, which argues for a second independent device); retiring the initial root token after proving a non-root admin login; scoping policies and tokens; and protecting storage, swap, and core dumps. Verify ships reasoned (no Vault instance) with the `sys/health` codes and a credential-free revocation check; row 2.30 tracks demonstrating it. Added `exposure-index.md` rows for 8200 and 8201 and cross-linked `machine-auth.md`. Every default verified at the vendor documentation.
- Added `self-hosted-ci-runners.md`, a guide for GitHub Actions runners and GitLab Runner (#130, row 2.12): a self-hosted runner exists to execute CI code, so the trust question is who can submit it, and both runners are outbound-only pollers with no inbound job-transport listener, so the controls are eligibility, credentials, executor privilege, and lifecycle rather than a bind address. GitHub's own guidance is to keep self-hosted runners off public repositories (a fork pull request can run dangerous code); runner groups are private-only by default, an ordinary fork `pull_request` run gets a read-only `GITHUB_TOKEN` and no other secrets, and the danger is host persistence plus the `pull_request_target` and `workflow_run` triggers. GitLab's privileged Docker executor is root on the host and mounting the Docker socket hands over the daemon's host even unprivileged, so untrusted work runs on an unprivileged Docker or Kubernetes executor, never shell or the socket, on disposable one-job workers. The guide separates the token classes (GitHub registration vs JIT `encoded_jit_config` vs the runner's local credential vs `GITHUB_TOKEN`; GitLab registration vs the `config.toml` authentication token vs `CI_JOB_TOKEN`) and notes the optional GitLab metrics (9252, no authorization) and session-server (8093) listeners. Verify is posture inspection since there is no listener to probe; the `config.toml` parse is run against fixtures while the runner-dependent checks ship reasoned, tracked by row 2.29. Added `exposure-index.md` rows for 8093 and 9252. Every default verified at the vendor documentation.
- Added `gitops-controllers.md`, a guide for Argo CD and Flux (#129, row 2.11): both reconcile git into a Kubernetes cluster, so both hold cluster-admin-equivalent power, and the guide leads with that shared blast radius, git as an authorization boundary, and credential scope. Argo CD stores its initial `admin` password in cleartext in the `argocd-initial-admin-secret` Secret (rotate with `argocd account update-password`, then delete it), ships a superuser `admin` to disable via `admin.enabled` after SSO, keeps anonymous access off by default (an anonymous caller gets `policy.default`), and binds the `argocd-manager` ServiceAccount to an admin-level ClusterRole; its API and UI are a ClusterIP Service (pod port 8080, metrics 8083) that the quickstart does not expose, and `--insecure` on the server versus `argocd login --insecure` on the client are two different TLS-skipping flags. Flux has no login surface at all, so the guide says so honestly: its surfaces are git credentials, the notification-controller webhook receiver on 9292 (where a `generic` receiver validates nothing, unlike `generic-hmac` and the provider types), and the kustomize and helm controllers that bind cluster-admin. The row's premise was corrected: the Argo CD quickstart does not expose the UI/API by default; exposure is operator-configured. Verify probes ship reasoned (no container runtime); row 2.28 tracks demonstrating them. Amended `exposure-index.md` 8080 and 9292. Every default verified at the vendor documentation.
- Added `supabase-self-hosted.md`, a guide for the self-hosted Supabase Docker stack (#128, row 2.7): `firebase-supabase.md` covers the row-level-security layer, but the self-hosted stack is a deployment of its own that ships demo secrets. Its `.env.example` sets a demo `JWT_SECRET`, pre-generated `ANON_KEY` and `SERVICE_ROLE_KEY` signed with it, a default `DASHBOARD_PASSWORD` of `this_password_is_insecure_and_should_be_updated`, and a placeholder `POSTGRES_PASSWORD`, and the vendor says never to start with these. An unedited `docker compose up` publishes the API gateway on `8000` (the whole REST/Auth/Storage/Studio surface, reachable with the publicly known demo keys, and `service_role` bypasses row-level security) and the Supavisor pooler on `5432` and `6543` (direct database connections on the demo Postgres password); the `db` service itself is not published. Because the demo `JWT_SECRET` is public, an attacker can forge any role until the secret and derived keys are regenerated together, so the guide covers regenerating every secret, keeping the two published surfaces private, the `service_role` bypass, and the Studio dashboard basic-auth. Verify probes ship reasoned (no container runtime); row 2.27 tracks demonstrating them. Amended `exposure-index.md` 8000 and 5432 and added 6543. Every default verified at the vendor documentation.
- Added `headless-browser-services.md`, a guide for headless browser automation backends (#127, row 2.6): Chrome DevTools Protocol, Selenium Grid, browserless, and Playwright server all hand remote control of a real browser over the network, so an exposed endpoint is usually unauthenticated code execution, cookie theft, and an SSRF pivot, not just data. The DevTools port (9222) has no authentication at all and binds loopback only until `--remote-debugging-address` or a published container port exposes it; Selenium Grid ships no auth by default and its bind is version-dependent (Grid 4 localhost, Grid 3 all interfaces), with the Docker images adding VNC/noVNC on 5900/7900 at the vendor example password `secret` and an event bus on 4442/4443; browserless (3000) is fully open when its `TOKEN` is unset; and the Playwright server's ws path is its only secret. The guide gives the bind, firewall, token, and fronting controls per tool and states honestly where no native auth exists. Verify probes ship reasoned (no container runtime); row 2.26 tracks demonstrating them. Added `exposure-index.md` rows for 4442/4443, 4444, 5900/7900 and 9222 and amended 3000 and 5555. Every default verified at the vendor documentation, including the Chrome 136 `--user-data-dir` change.
- Added `headless-cms-instant-api.md`, a guide for instant-API backends (#126, row 2.5): Strapi, Directus, Hasura, and PostgREST turn a schema into a ready-made API, so one control gates the whole datastore, and it fails two ways. Strapi and Directus keep their data API closed by default (Strapi content types are private until made public; Directus public permissions are off by default), so their risk is the admin and bootstrap surface: Strapi's first admin-panel visitor becomes the administrator with no default password, and Directus takes `ADMIN_EMAIL`, `ADMIN_PASSWORD` and a static `ADMIN_TOKEN` from the environment. Hasura exposes the full GraphQL API and console with admin rights when `HASURA_GRAPHQL_ADMIN_SECRET` is unset and binds every interface by default; PostgREST's access is exactly the database grants held by `db-anon-role`, and it binds all IPv4 interfaces with `db-schemas` defaulting to `public`. The Verify probes discriminate on response content and ship reasoned because the authoring environment has no container runtime; row 2.25 tracks demonstrating them live. Added `exposure-index.md` rows for 1337 and 8055 and amended 3000 and 8080. Every default was verified at the vendor documentation while drafting; an unverifiable Directus `KEY`/`SECRET` claim was dropped and a stale Directus source URL corrected.
- Promoted the sidenav group labels to headings in `site/index.html` (#125, row 4.2): the fourteen menu group labels (`On this page`, the twelve guide categories, and `Reference`) were `<p class="sidenav-h">`, so screen-reader heading navigation skipped every one and the menu carried no heading structure. They are now `<h2 class="sidenav-h">`, which gives a valid non-skipping outline: the page's single `<h1>`, then these group headings inside the labeled `<nav>` landmark, then the `<main>` section headings. The `.sidenav-h` rule targets the class, so the appearance is unchanged and the change is markup only, with no inline block touched (no CSP hash impact). Verified 14 headings render and no `<p class="sidenav-h">` remains.
- Extended the local-link gate to `requests/*.md` (#124, row 4.6): the gate scanned only root `./*.md` and resolved link targets relative to the repo root, so the links in `requests/README.md` and `requests/TEMPLATE.md` (the first sanctioned non-root Markdown, added in #82) went unchecked. The gate now also walks `requests/*.md`, resolving each target relative to the file's own directory, so a same-directory link and a `../` parent link both check the right path. An absolute target (a leading `/`) is rejected rather than silently resolved against `requests/`, since `requests/` links are repo-relative by convention. Confirmed by mutation that a broken `requests/` link and an absolute target are both caught while ordinary same-directory and parent links pass.
- Flagged Milvus's published MinIO in vector-databases.md (#123, row 1.73): the per-vendor check the #122 sweep deferred. Milvus's standalone `docker-compose.yml` publishes its bundled MinIO object store to the host on `9000` (S3 API) and `9001` (console) on every interface with the default `minioadmin` login, beside Milvus, while etcd stays internal. The guide now says to loopback-map or firewall those ports and change the MinIO login, since authenticating Milvus does nothing for a MinIO published next to it. Airflow and Temporal keep their datastores internal (`expose`, not `ports`), so no note was needed there. Verified at each project's Compose file.
- Swept the Compose backing-store pointer question (#122, row 1.43): checked every compose-deploying guide for the "authenticated front door with a published backing store" pattern. It is carried authoritatively in ai-infra-services.md (Dify, Mem0, Onyx, cross-referenced by agent-builders.md) or handled inline where it recurs (llm-observability.md already covers Langfuse's bundled MinIO), and the vector-database, orchestrator, chat-UI, and n8n guides lead with bind-privately guidance, so no cross-reference additions were warranted. A per-vendor residual (whether the Milvus, Airflow, and Temporal vendor Compose files publish a datastore beside the app) is filed as row 1.73.
- Added a file-access section to gradio.md (#121, row 1.26): `launch()` serves files over a `/gradio_api/file=` route; by default it serves `set_static_paths` files, `allowed_paths`, and its cache, and a path the app returns from a function is also served if it lies in `allowed_paths`, the working directory, or the temp directory, with the cache reachable by every user. The new section favours an allowlist over a blocklist: launch from a directory with no secrets, scope `allowed_paths`/`set_static_paths` narrowly, treat `blocked_paths` as a fragile backstop (filesystem-quirk bypasses of path blocklists have occurred) and keep Gradio current, and never return user input through a file-based output component. A Verify check requests an allowed and a blocked marker over the file route while signed in. Verified at Gradio's file-access guide.
- Named template remote-desktop listeners in gpu-clouds.md (#120, row 1.35): a rented-GPU image built around ComfyUI or a Linux desktop may include a noVNC web interface (a port like 6080) over a VNC server (5900 or 5901), either of which may default to a weak or empty password and be published like any other listener. A new Common-mistakes bullet says to check both listeners for weak or missing authentication, gives a direct VNC mapping its own password and an encrypted tunnel or keeps it off the public mapping, and probes the raw VNC port with a VNC client, since the browser page's HTTPS does not protect a separate VNC connection.
- Established the vendor Compose facts for agent-builders.md (#119, row 1.41): Flowise, Langflow, and LibreChat each ship an official Compose file that publishes the app port on all interfaces (`${PORT}:${PORT}` for Flowise and LibreChat, `7860:7860` for Langflow), and LibreChat's is meant to be extended through a docker-compose.override.yaml. So an override must replace the base publication (`ports: !override [...]`), not add a loopback entry beside it, since a plain `ports` list merges; the `!reset []` form shown for Dify clears a publication entirely instead. Also noted that LibreChat's file publishes an `admin-panel` on `${ADMIN_PANEL_PORT:-3000}:3000` and Langflow's publishes PostgreSQL on `5432:5432` beside the app. Verified at each project's Compose file (added to Sources).
- Added a worked pre-15 view REVOKE example to firebase-supabase.md (#118, row 1.40): the Supabase section described revoking an API-exposed view from public, anon, and authenticated on PostgreSQL 14 and earlier but showed no SQL. Added a `sql` block with the revoke and a `has_table_privilege` check for both anon and authenticated (the prose confirmed only anon), each of which must return f. Per-overload RPC negative tests were already present in the Verify section.
- Clarified the tailnet access boundary in tailscale.md (#117, row 1.39): the guide said `serve` traffic is "reachable only by devices in your tailnet, authenticated by device identity and your tailnet ACLs", without noting that a new tailnet is allow-all by default (`src` `*`, `dst` `*:*`), so every device and user in the tailnet reaches the service until the policy is narrowed with a grant or ACL from a specific source to this host and port. It adds that tailnet access is network reachability, not user authorization: a panel behind `serve` still needs its own login. Verified at Tailscale's access-control docs (added to Sources).
- Added the DOCKER-USER chain and a Docker 28.0 caveat to docker.md (#116, row 1.6): the guide offered only loopback publishing for a host-reachable port. It now notes that the host-IP restriction in a publish string (including `127.0.0.1`) is reliable only on Docker Engine 28.0 and later, since before 28.0 a neighbour host could reach a loopback-mapped port, a remote host could reach a port bound to a specific host IP, and an unpublished container port was reachable by direct routing (all three fixed in 28.0). For a port that must be published on a routable interface, it adds the `DOCKER-USER` iptables chain (evaluated before Docker's own accept rules) with a source-IP filter, and the caveat that after DNAT those rules match a container's internal address, not the published port. The DOCKER-USER documentation has moved off the packet-filtering page the guide cited; the correct Docker "with iptables" page and the 28.0 release notes were added to Sources.
- Added an Adminer section to admin-uis.md (#115, row 1.32): Adminer is database management in a single PHP file at a predictable path (`adminer.php`) that scanners probe constantly, and it keeps no accounts of its own; its login form takes the database server's own address, username, and password, so an internet-reachable Adminer is a public login form onto the database and a perennial exploit target. The section applies the guide's never-public rule (private, proxy-level TLS and authentication, source-IP restriction) and says to delete the file from the docroot when it is not in use. Verified at adminer.org.
- Named credential-bearing config files as secrets in secrets.md (#114, row 1.33): a new rule 9 says a Terraform state file (`terraform.tfstate`, plaintext, holding any secret in the configuration), a kubeconfig (a base64 client private key in `client-key-data`), and `~/.docker/config.json` (registry logins base64-encoded without a credential helper) belong in `.gitignore` beside `.env` and `*.pem`, and that the rule-4 scanners flag one that slips in. Verified at the Terraform, Kubernetes, and Docker docs (added to Sources).
- Stopped the ollama.md and memcached.md Verify steps filtering ss output by port (#113, rows 1.28 and 1.29): both piped `ss` through `grep <port>`, which the repo's own rule (apache.md, traefik.md) forbids because a port filter hides an unexpected listener and can match the port's digits inside an address such as an IPv6 literal. They now run the unfiltered `ss -tlnp` (ollama) and `ss -tlnup` (memcached, which also confirms no UDP line), so the reader reads the whole listener table and confirms the service is on the intended address with nothing unexpected beside it.
- Generalized the CSP-hash gate to cover site/404.html (#112, row 3.12): `tools/check_csp_hashes.py` hard-coded site/index.html and modeled one `<style>` and one `<script>`, so 404.html (added in #102, its style hash pinned in the shared `/*` CSP) was never verified; editing it left the gate green while a browser refused the style. The gate now checks a declared per-page list, refuses any unlisted `site/**/*.html`, and requires exactly one CSP under `/*` with no other rule setting or detaching one. The mutation harness grew from 43 to 68 cases, including an edited-but-unrepinned 404 style that must fail. Orphan-pin exactness is deferred to row 3.15. Tier Sensitive: tri-family seeds combined by Fable, premises re-verified at source, adversarial self-checks confirmed the fail-open is closed and the harness catches a single-page regression.
- Added a homepage copy-paste prompt for confidential business rules (#111, row 4.11): secrets.md rule 8 treats a confidential pricing formula, allocation rule, or customer-specific process as a secret that belongs out of public repositories, client bundles, and public LLM prompts, but no homepage task surfaced them. A new "Audit for exposed business rules" prompt card directs an assistant to secrets.md and web-exposure.md, has it search the public repository and git history, anything shipped to the browser (a NEXT_PUBLIC_ or VITE_ value, client-shipped code, or a source map), and any prompt sent to a public LLM, and apply the per-guide fix only after approval. It matches the five existing prompt cards and adds no inline script or style, so the CSP hashes are unchanged. Completes a second external review (item 6).
- Named the OWASP Top 10 for LLM Applications in the scope statements (#110, row 4.10): README.md and CONTRIBUTING.md already said general application security (injection, deserialization, business logic) is out of scope and pointed to OWASP resources; each now also points to the OWASP Top 10 for LLM Applications (https://genai.owasp.org/llm-top-10/) for LLM-specific application risks such as prompt injection, so a reader of these AI-deployment guides knows where the application-layer LLM risks are catalogued. One link per file, no new guide; the repo's own scope stays deployment exposure. Found in a second external review (item 5).
- Added cross-user isolation Verify steps to vector-databases.md and chat-uis.md (#109, row 1.70): the guides configured tenant row-level security and per-workspace access but had no step proving one user's RAG or chat query does not return another user's data. vector-databases.md now has a cross-tenant check (as tenant B, query, confirm only B's rows; an RLS-bypassing role returns both, so the difference is the policy working, not an empty table); chat-uis.md now says to ask, as user B, a question whose answer lives only in user A's documents and confirm nothing of A's returns. Found in a second external review.
- Added two secret-handling principles to secrets.md (#108, row 1.69): keys and customer data stay out of AI-model prompts, including the system prompt and any internal role map (a system prompt is not a secret store, and prompt injection can make a model repeat it); and unpublished business rules (pricing, allocation, a customer-specific process) are treated like secrets, kept out of public repositories, client bundles, and public LLMs. Found in a second external review.
- Added browser-side cache guidance to headers.md (#107, row 1.68): the guide covered a shared cache serving user A's authenticated response to user B, but not the browser's own cache. It now explains that `private` still lets the browser store a page (so the Back button can redisplay an authenticated view after logout), that `no-store` is needed on authenticated or session-cookie responses, and that logout should send `Clear-Site-Data` to clear the browser and back/forward cache; the Verify adds a back-button-after-logout check. Verified at MDN. Found in a second external review.
- Brought the legacy probe guards to the rule-6 template (#106, row 1.44): eleven guides carried the pre-hardening guard, an unquoted placeholder on the `set --` line, no `PASTE_WHOLE_BLOCK` sentinel or arity check, and no `-g` on the curl probe. Each guard now uses the full CONTRIBUTING rule-6 shape: a single-quoted placeholder with the substitute-inside-the-quotes note, the sentinel-and-shift check, an `[ "$#" -eq 1 ]` arity check, `case "$1"`, and `-g` on every probe curl. shellcheck and the guard-conventions gate (#105) verify them. From the guard-conventions review.
- Gated the curl guard conventions in tools/check_guard_conventions.py (#105, row 3.11): a stdlib shell-lexer gate (not line-regex) that flags a fenced-block curl whose first argument is not `-q`/`--disable` (curl only skips `~/.curlrc` when `-q` leads) and an unglobbed `[...]` or `{...}` in a scheme-bearing URL without `-g`. Registered in run_all_checks.sh with `--no-c2` (the probe-outside-guard rule mis-flags the corpus's legitimate exit-guard-then-probe-after-esac idiom, deferred to row 3.14) and `--min-curls 176` as a scope-regression tripwire; a 60-case self-test guards the gate itself. Enablement on main is clean (181 curls, 0 findings). From the guard-conventions review.
- Added a leading `-q` to a devops-uis.md TLS probe (#104, row 1.67): one curl in the Docker-over-TLS Verify block (the missing-client-certificate handshake check) did not lead with `-q`, so a reader's `~/.curlrc` could silently alter it, while the next curl in the same block already did. Prepended `-q`. This is the surviving genuine defect from the guard-conventions-gate review; the two files it originally named were fixed in later pull requests.
- Synced the plugin manifest version to VERSION (#103, row 3.13): `plugin/plugin.json` had been frozen at `1.0.38` since the plugin was first packaged (#39) while the repository moved to `1.0.102`, so an adopter's updater keyed on the plugin version saw no reason to re-fetch corrected guides. `scripts/build-plugin.sh` now rewrites the manifest version from VERSION on every build, and the existing whole-corpus staleness gate (which rebuilds the bundle and diffs) enforces it, so no separate version gate is needed. Found in an external pre-launch review.
- Added a site 404 page in site/404.html (#102, row 4.9): unknown URLs were served the homepage with a 200; Cloudflare Pages serves 404.html for an unmatched path when the file exists. The page matches the site (brand, accent, theme-aware) and is self-contained with one inline stylesheet, whose sha256 is pinned in site/_headers so the site CSP allows it. The CSP gate still covers only index.html; extending it to 404.html is tracked as row 3.12. Found in an external pre-launch review.
- Added a security policy in SECURITY.md (#101, row 4.8): the repository had none. It routes reports to GitHub private vulnerability reporting (with a fallback if that is not enabled), scopes reports to secureconfig's own guidance and code rather than to vulnerabilities in the products the guides document, and notes that fixes land on main so an installed plugin bundle or a copied guide must be refreshed to get them. SECURITY.md is added to the gate suite's not_a_guide exclusions so a meta file is not held to guide shape. Found in an external pre-launch review.

## 2026-09-14
- Guarded the external-reachability probes in ray.md and mlflow.md (#100, row 1.66): each probe used an unguarded `http://REPLACE_WITH_THE_SERVER_PUBLIC_IP:PORT/` with a pass of `http=000` and `time_connect=0.000000`, but an unsubstituted placeholder fails DNS to exactly that result, a false pass indistinguishable from a filtered port (CONTRIBUTING rule 6). Both probes now use the corpus's guarded subshell idiom (a `case` on `*REPLACE_WITH_*` refuses to run unsubstituted, `--noproxy '*'` so a proxy cannot answer, and the `exitcode`/`errormsg` write-out naming why the connection failed), the same pattern already in chat-uis.md and others; the curl Sources bullets now cite the exitcode/errormsg variables (curl 7.75.0). Verified with shellcheck. Found in an external pre-launch review.
- Corrected vLLM's default bind in model-servers.md (#99, row 1.65): the intro listed only TGI and Triton as `0.0.0.0` binders, implying vLLM defaults local, but `vllm serve` leaves `--host` unset, which binds every interface (the startup log shows `http://0.0.0.0:8000`). The guide now lists vLLM among the all-interfaces binders and tells readers to pass `--host 127.0.0.1`, with the launcher source. Verified at the vLLM launcher source. Found in an external pre-launch review.
- Noted the Ollama Docker image's all-interfaces default in ollama.md (#98, row 1.64): the guide said Ollama binds `127.0.0.1` by default, true for the standalone binary, but the official `ollama/ollama` Docker image sets `ENV OLLAMA_HOST=0.0.0.0:11434`, so a containerized Ollama listens on every interface and a bare `-p 11434:11434` republishes it to every host interface. The guide now adds that in Docker the control is the published address (`-p 127.0.0.1:11434:11434`), not `OLLAMA_HOST`, because the container must listen broadly for a proxy to reach it. Verified at the ollama/ollama Dockerfile. Found in an external pre-launch review.
- Warned about the OpenSearch demo internal_users.yml accounts in elasticsearch.md (#97, row 1.63): the demo security configuration seeds seven built-in accounts (`admin`, `anomalyadmin`, `kibanaserver`, `kibanaro`, `logstash`, `readall`, `snapshotrestore`), and `OPENSEARCH_INITIAL_ADMIN_PASSWORD` sets only the `admin` password, so the other six keep the credentials shipped in the demo file. The guide now says to replace `internal_users.yml` or remove the demo accounts before any real deployment and to confirm each demo login is refused. Verified at the opensearch-project/security internal_users.yml. Found in an external pre-launch review.
- Warned against the Airflow docker-compose defaults in workflow-orchestrators.md (#96, row 1.62): the official `docker-compose.yaml` selects the FAB auth manager but seeds it with a known admin (`_AIRFLOW_WWW_USER_USERNAME` and `_AIRFLOW_WWW_USER_PASSWORD` both `airflow`) and signs API tokens with a fallback `AIRFLOW__API_AUTH__JWT_SECRET` of `airflow_jwt_secret` (used when the variable is unset), and it publishes port 8080 on all interfaces; its own header calls it local-development only. A reader who runs it unchanged gets an `airflow`/`airflow` login exposed wherever 8080 is reachable and can forge API tokens to impersonate existing users. The guide now names all three, says to set your own values before the container is reachable, and to publish 8080 only behind the fronting layer. Verified at the apache/airflow docker-compose.yaml. Found in an external pre-launch review.
- Warned against the Langfuse self-hosting compose defaults in llm-observability.md (#95, row 1.61): the official `docker-compose.yml` ships example secrets marked `# CHANGEME`, and `NEXTAUTH_SECRET` protects the session token, so the shipped `mysecret` lets anyone mint a session for any existing account (admins included), skipping the sign-in flow and its SSO and MFA; `SALT` and an all-zero `ENCRYPTION_KEY` (which encrypts stored provider keys and integration secrets) ship the same way, and the compose bundles a MinIO with `minio`/`miniosecret` published on host port 9090 plus langfuse-web on 3000. The guide now names these, tells the reader to generate a fresh random value for each before first start, and to keep the bundled stores off untrusted networks. Verified at the langfuse/langfuse docker-compose.yml. Found in an external pre-launch review.
- Enabled mongo-express basic auth across 1.x in admin-uis.md (#94, row 1.60): the guide set ME_CONFIG_BASICAUTH_USERNAME and ME_CONFIG_BASICAUTH_PASSWORD, but 1.x has basic auth off by default and credentials alone do not enable it, so a reader ran the panel with no login. Which variable enables it also changed within 1.x: 1.0.x reads ME_CONFIG_BASICAUTH, 1.1.0 and later read ME_CONFIG_BASICAUTH_ENABLED, so the block now sets both, and the intro notes that admin:pass is the credential fallback if you skip the username and password. Verified at the mongo-express v1.0.2 and v1.1.0 config source. Found in an external pre-launch review.
- Flagged the KRaft controller port overlap in kafka.md (#93, row 1.59): the guide puts the SASL_SSL broker listener on 9093, which is the port Kafka's default `server.properties` assigns to the KRaft controller listener, so on a combined broker-and-controller node the two try to bind 9093 and the broker fails to start. Section 1 now warns to give the controller a different port. Verified against Kafka's default config. Found in an external pre-launch review.
- Bound the LiteLLM proxy to loopback explicitly in litellm.md (#92, row 1.58): the guide said to run the proxy on loopback but the proxy's `--host` defaults to `0.0.0.0`, so a reader who ran it without the flag bound every interface. The guide now shows `litellm --host 127.0.0.1 --port 4000 --config config.yaml` and adds the LiteLLM CLI source. Verified at docs.litellm.ai. Found in an external pre-launch review.
- Broadened the leaked-key search in firebase-supabase.md (#91, row 1.57): the Verify step searched the client bundle for the word `service_role`, but a leaked key is a value, not that word, so the search missed it. A Supabase secret is a legacy JWT starting `eyJ` or a new `sb_secret_` string; the step now greps for `service_role|eyJ|sb_secret_` and notes a hit must be rotated. Verified at supabase.com API keys docs. Found in an external pre-launch review.
- Hardened the sshd config ordering guidance in host.md (#90, row 1.56): sshd uses the first value it reads for each option and includes `/etc/ssh/sshd_config.d/*.conf` at the top of the main file, so a cloud image's `50-cloud-init.conf` (`PasswordAuthentication yes`) silently overrides a setting placed later, leaving password login on. The guide now puts the hardening in a first-sorting `00-hardening.conf` drop-in, explains the override, and verifies the EFFECTIVE config with `sudo sshd -T` rather than only the `sshd -t` syntax check. Found in an external pre-launch review.
- Validated manifest file names in the plugin updater (#89, row 4.7): scripts/update-guides.sh looped over the fetched MANIFEST.sha256 and wrote each entry to `$work/$name` with no name check, so a manifest entry such as `../x.md` escaped the temporary directory and the subsequent digest check still passed. The loop now rejects any name outside `[A-Za-z0-9._-]`, containing `..`, or beginning with a dot, refusing the whole update so a malicious manifest cannot write outside its temp directory. Found in an external pre-launch review.
- Warned that Open WebUI's first account is the administrator in open-webui.md (#88, row 1.55): the guide covered ENABLE_SIGNUP and DEFAULT_USER_ROLE=pending, which governs only later accounts, but not that the first account created is made admin regardless. On an exposed instance with signup on, whoever registers first owns it, so claim the admin account on loopback first (or preset WEBUI_ADMIN_EMAIL/WEBUI_ADMIN_PASSWORD). Verified at the Open WebUI FAQ. Found in an external pre-launch review.
- Completed the nginx Authelia identity headers in fronting-auth.md (#87, row 1.54): the protected-location snippet called `auth_request` but omitted the `auth_request_set`/`proxy_set_header Remote-User` (and Groups/Name/Email) pairs from Authelia's `authelia-authrequest.conf`. Since nginx forwards client request headers to the upstream by default, without them a client that reached the app could send its own `Remote-User: admin`; the pairs set the identity from Authelia's response and overwrite the client's. Verified against Authelia's nginx integration guide. Found in an external pre-launch review.
- Marked the self-signed leaf certificate CA:FALSE in self-signed.md (#86, row 1.53): the `openssl req -x509` commands set no `basicConstraints`, so on configurations where OpenSSL marks the leaf `CA:TRUE` the certificate, once installed into a client trust store in section 4, becomes a trusted signer for any name using the unencrypted key `-nodes` leaves on disk. Both commands now pass `-addext "basicConstraints=critical,CA:FALSE"`, with a bullet explaining why. Found in an external pre-launch review.
- Fixed a literal placeholder password in rabbitmq.md (#85, row 1.52): the account block created the `ops` administrator with the literal password `...`, which a reader copying the block would deploy. It now uses a `REPLACE_WITH_A_DIFFERENT_LONG_RANDOM_PASSWORD` placeholder like the `app` user above it. Found in an external pre-launch review.
- Added Webhook-node authentication to n8n.md (#84, row 1.24): a private Webhook node left on the default None answers anyone who reaches its URL, and this is separate from the instance login and the `X-N8N-API-KEY` public API, so the existing api/v1 check does not establish it. The guide now sets the node Authentication to Basic, Header, or JWT auth, notes it is per-node, points to signature validation for third-party callers, and adds a Verify probe that a known production webhook (`/webhook/<path>`) rejects an unauthenticated request.
- Bound Express `trust proxy` to the proxy topology in nodejs.md (#83, row 1.23): the guide set it to a bare hop count of 1; a blanket `true` or an over-large hop count lets a client forge X-Forwarded-For/Host/Proto (faking the client IP, hostname, or HTTPS status) unless the last trusted proxy overwrites them. It now trusts `loopback` for a same-host proxy, with the exact IP or subnet shown for a remote one, explains the affected req.* properties and the shorter-path risk, and adds a Verify probe that a forged X-Forwarded-For is not reflected in req.ip.
- Added a generic controls reference and a guide-request path (#82): controls-reference.md distils the control patterns that recur across the corpus (network binding, hidden listeners, default credentials, registration, MFA, TLS, file access, secrets, API surfaces, container posture, and the Verify discipline) into an alignment checklist for applications with no guide of their own, and a new requests/ directory with a README and template lets anyone, including an AI assistant, request a guide by pull request. README point 3 points assistants at both. The reference is excluded from the guide-shape gate as a cross-service document rather than a service guide.
- Added a GitLab CE section to devops-uis.md (#81, row 1.13): the root password auto-generates into /etc/gitlab/initial_root_password and is deleted on the first container restart after 24 hours, so sign in and rotate it; signup_enabled defaults true (clear "Allow new user accounts" for a private forge); default_project_visibility and default_group_visibility both default private, and "internal" means any signed-in non-external user; admin-enforced 2FA. Adds a Verify check for the initial_root_password file and three docs.gitlab.com sources.
- Removed the dated section headings from DECISIONS.md and added an intro paragraph (#80): the rulings now read as one standing set rather than a dated log, and the intro records the DECISIONS.md convention across the maintainer's projects, so an assistant can predict how he decides and a settled question is not asked twice.
- Simplified the GRC Library entry in the site's related-resources note (#79): a single "GRC Library" link to grclibrary.ai, in the same style as the AIQT link, replacing the repository link and the separate URL. The repository is still reachable from the GRC gap-assessment prompt.
- Corrected the frp OIDC directionality note in tunnels.md (#78, row 1.51): auth.method = "oidc" has frpc obtain a token from an OIDC provider through the Client Credentials Grant and frps validate it by issuer and audience, authenticating frpc to frps rather than "both sides" as the line previously read. Found by codex during the #77 QA.
- Covered ssh -R remote forwarding in tunnels.md (#77, row 1.30): the server's sshd_config GatewayPorts governs the forwarded port's bind (default no keeps it on loopback and overrides a client's public-bind request; yes forces a wildcard bind; clientspecified honours the client's requested address), and SSH authenticates the tunnel session but not callers to the forwarded port, so a widened GatewayPorts in front of an app with no login of its own publishes it unauthenticated.
- Covered Caddy's admin API in caddy.md (#76, row 1.27): it listens on localhost:2019 with no auth and can replace the whole config or stop the server, so it must never be published; the guide adds the permissioned-unix-socket pattern for shared hosts and a Verify check for the 2019 listener.
- Warned against quick-sharing files with python -m http.server in python.md (#75, row 1.31): it binds every interface by default and serves the current directory (source, .env, keys, dumps), follows symlinks, and is not for production; the guide shows the loopback-bound alternative.
- Added MySQL's X Plugin listener to mysql.md (#74, row 1.21): MySQL 8.4 enables the X Plugin by default on port 33060 with mysqlx_bind_address defaulting to every interface, independent of the classic bind_address; the guide now sets mysqlx_bind_address (or mysqlx = OFF) and checks 33060.
- Covered Sidekiq Web UI authentication in ruby.md (#73, row 1.20): the usual mount exposes job arguments and queue controls to anyone who reaches /sidekiq; added the Devise authenticate constraint and the Rack::Auth::Basic pattern, plus a Verify probe of the path.
- Covered directory listing in web-exposure.md (#72, row 1.19): Debian and Ubuntu ship apache2.conf with Options Indexes on /var/www, so a directory without an index.html is served as a browsable listing that defeats the name-based deny rules; added an Options -Indexes block, a Verify probe against a directory URL, and the mod_autoindex and nginx autoindex sources.
- Added RabbitMQ's epmd (default 4369) and Erlang distribution (default 25672) ports to rabbitmq.md (#71, row 1.17): the shared Erlang cookie is the only default credential on the distribution port and grants full control of the node, so a weak or leaked cookie on a published port is a remote compromise; the ss check and exposure-index.md now cover both ports.
- Named MinIO's web console listener and checked its port (#70, row 1.16): the embedded console is a second listener (--console-address / MINIO_CONSOLE_ADDRESS) that binds all interfaces when host-less, even with the S3 API on loopback; the guide now advises --console-address 127.0.0.1:9001, checks 9000 and 9001, probes the console, and adds a 9001 exposure-index row.
- Added a text-generation-webui section to model-servers.md (#69, row 1.8): its two independently keyed surfaces (Gradio UI and the OpenAI-compatible API), the --listen-widens-both trap, the --share/--public-api tunnels, per-surface auth, the validate_host_header 400, and a reasoned /v1/models probe (demonstration tracked by row 1.50). Flags verified at source.
- Added the curl 7.75.0 version note to the Verify probes in llm-observability.md and vector-databases.md (#68, rows 1.48/1.49); both print the exitcode and errormsg write-out variables that curl added in 7.75.0.
- Prepended `-q` to two Verify probes (#66) in `fronting-auth.md` and `image-gen-uis.md` so the reader's `~/.curlrc` cannot silently alter them; surfaced by the row 3.11 guard-conventions gate work.
- Rotated row 1.12 into `DONE.md` (#65) after #64 merged, and opened three follow-up rows: 1.47 (live exposed/fixed reproduction of the Traefik canary probes) and 1.48/1.49 (the curl 7.75.0 note that `llm-observability.md` and `vector-databases.md` still lack for their `exitcode`/`errormsg` write-out variables, found while applying #64).
- Disabled Traefik's Docker-provider default exposure (`traefik.md`, #64): added the `providers.docker` block with `exposedByDefault: false`, the read-only socket mount with its root-equivalence note, the opt-in/opt-out and daemon-wide-discovery prose, an unlabelled-canary front-door probe with an app baseline, a widened whole-table `ss` check, and the Swarm-scope note. Closes backlog row 1.12.

### Added

- The Phoenix administrator bootstrap in `llm-observability.md` (#62). Enabling Phoenix authentication as the guide previously advised left the default `admin@localhost` / `admin` login live; the guide now sets `PHOENIX_DEFAULT_ADMIN_INITIAL_PASSWORD` (read only on the startup that first creates the account, and silently inert afterward), changes the password at the UI before the instance is reachable and proves the default is dead, adds an unauthenticated read (`/v1/projects`) Verify probe in the hardened guard idiom and a manual default-credential check, governs the OTLP write path through the listener inventory rather than a curl probe, and records that Phoenix has no native MFA but federates to an OAuth2/OIDC provider. Closes backlog row 1.11.
- Qdrant's internal cluster port 6335 in `vector-databases.md` (#63). An API key does not protect 6335 (the vendor states internal channels are never protected by a key), and the guide had listed 6335 in its port table but omitted it from the Verify grep. The guide now annotates 6335 as distributed-mode-only, states that a key and `read_only_api_key` do not cover it, restricts it at the network layer with `cluster.p2p.enable_tls`, replaces the forbidden `ss` grep with a whole-table inventory, and probes 6333/6334/6335 from outside the peer allowlist with exit/err discrimination. The probes are marked reasoned (no distributed cluster in the authoring environment), tracked by row 1.46. Closes row 1.9.

## 2026-09-13

### Added

- A required gate on the changelog's own pull-request coverage (#53). It compares every `(#N)`
  squash-merge suffix in the local history against the `#N` references here, so it reads the
  repository and nothing else and keeps the suite offline. It found two unreferenced pull requests
  on its first run against this branch. It exempts exactly one, the highest-numbered merged pull
  request, because a pull request cannot reference its own number before it is opened; let a second
  merge without its entry and the gate goes red. It fails on a shallow clone rather than skipping,
  because with one commit it would find nothing missing and report a pass.
- The pack's `cntdef` rule, "Continue by default", vendored verbatim at
  `.aiqt/core/rules/trust-continue-by-default.md`, with `CLAUDE.md` and `AGENTS.md` pointing at it
  (#55). An earlier attempt paraphrased the rule into those two files instead and the permission
  classifier refused it three times, including a read-only check: prose that narrows when an agent
  pauses for its maintainer, authored by that agent into the file that configures it, is the shape
  it screens for whatever its provenance. Vendoring the upstream file is both permitted and the
  correct architecture. The file is byte-identical at this repository's pinned commit and at
  upstream main, so it needs no pin bump.
- `ai-infra-services.md`, the 86th guide (#60). One guide covering SearxNG, LocalAI, Text Embeddings
  Inference, LangServe, Mem0 and Onyx, closing six backlog rows. Three of those rows had premises
  that were wrong at source: LangServe is deprecated, and Mem0 and Onyx both ship authentication on.
  What the six share is not a missing password but that the run form decides what the network sees,
  and it publishes more than the authentication covers. Two shapes are named: authentication absent
  or unset, and a guarded front door with the backing store published beside it. Text Embeddings
  Inference was reclassified during review: it does have a native control, `--api-key` with the
  environment variable `API_KEY`, off by default, and a Prometheus listener on port 9000 that the
  quick tour never mentions and the key does not cover. Mem0 and Onyx both hand administrator rights
  to whoever registers first, so the bootstrap has to happen before the deployment is reachable from
  anywhere but its own host.

### Fixed

- A sixth way past the copy-paste guard, found while testing the fifth (#60). The five earlier
  designs each constrained what happened after the guard ran; this one lands before it exists. The
  placeholder sat unquoted at its substitution site, so a reader pasting a URL with a query string
  had the ampersand background the `set --` itself, leaving the parameters empty while the rest of
  the line ran as its own command, and a value containing `$(...)` or a backtick simply executed.
  Every substitution site is now a single-quoted literal, which a guard cannot achieve because the
  shell evaluates the value first. Demonstrated three ways in bash; the fix is verified against
  bash, dash and BusyBox ash, with and without `set -u`, with the caller's own arguments intact.
- The six guards in the new guide, which the corpus-wide sweep had missed (#60). The sweep keyed on
  the variable name the other twelve files used, and this guide uses its own, so it shipped the form
  a `readonly` variable in the reader's shell defeats. Three `curl` invocations went the same way,
  sitting on indented continuation lines the sweep's line-anchored pattern never matched, and have
  gained the `-q` the rest of the corpus carries.
- The guard's seventh bypass, found by round-2 review after the sixth was fixed in the same change
  (#60). A block pasted without its `set --` line inherits whatever positional parameters the
  reader's shell already held, and a stale pair from an earlier experiment satisfies every check the
  guard makes, so the probe fires at the old target and its refusal is read as this target's
  evidence. Counting the values does not close it, because a shell holding the expected number
  passes the count; that was measured against the first fix, which is why the fix that shipped is a
  sentinel the block sets and shifts away. The whole historical bypass set was re-run against the
  sentinel rather than reasoned about: unsubstituted placeholder, stale-positional partial paste,
  `readonly`, `declare -i`, an exported value, `IFS`, caller arguments present, and a placeholder
  embedded in a longer value, across bash, dash and BusyBox ash under `set -u`. Two limits are now
  stated rather than papered over: a shell whose `set` has been shadowed defeats every guard at
  once, and a value containing an apostrophe cannot be carried inside the quotes.
- The Onyx development-form port inventory, which was short by two published ports (#60).
  `docker-compose.dev.yml` is an override, and its own header gives the launch form as
  `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --wait`, so the effective
  configuration includes the base file, which publishes nginx on `${HOST_PORT_80:-80}:80` and
  `${HOST_PORT:-3000}:80`. A reader probing exactly the ports this guide enumerated would have read
  "Fixed" with two ports still published. The same file settles a question this guide had recorded
  as unverified: host 3000 reaches nginx's container port 80, and `web_server` carries no host
  publication of its own.
- Eight band-1 errors across eight guides (#45), closing `TODO.md` band 1 to empty. Each was a
  guide asserting something false or a Verify step that could not discriminate: a missing
  `--cacert` that made an Elasticsearch check die on TLS rather than answer; a Tailscale claim that
  ignored how the fronted application binds; `ufw allow OpenSSH`, which admits the whole internet
  while `cloud-firewalls.md` rule 3 says SSH is not public; a PostgreSQL Verify step that could not
  tell a refused password from a refused connection; a Dify paragraph that implied a setting could
  unpublish a port the vendor Compose file publishes unconditionally; a cloud-firewall Verify step
  with no runnable command for any provider; curl -q exit 7 or 28 treated as proof of egress
  enforcement; and a Supabase section that never warned that views and `SECURITY DEFINER`
  functions run with their owner's rights, past RLS.
- Five QA rounds across three families produced the shipped text. Rounds 1 and 3 rejected, round 4
  accepted with changes, and round 5's single finding rested on a premise the guide did not state.
  Every finding was checked at its vendor source before it was applied, and two were rejected on
  evidence: a recollection that Dify's Compose file binds the plugin daemon to localhost, and a
  report that injecting `curl -k` survives the gate suite. Neither held.
- Three of the fixes had to be fixed again during review, all the same shape: removing a narrow
  allowance and leaving the broad one. A ufw rule added beside the old `allow OpenSSH`; a firewalld
  rich rule added beside an existing `--add-service=ssh`; and a Supabase view revoked from `anon`
  and `authenticated` while `PUBLIC` still granted it. The last was demonstrated on a live
  PostgreSQL: `has_table_privilege` stayed true and the view kept returning rows.
- The gate suite caught no semantic defect in any round, which is what the tri-family tier exists
  for. It did catch one defect in the original draft: SC2016 on a JMESPath backtick literal, which
  inside a shell command is a command substitution waiting to happen.
- `mlflow.md` and `ray.md` stopped reading a bare `--max-time` timeout as proof that a connection
  was blocked (#49), which `egress-metadata.md` had just said plainly it is not. Demonstrated with
  curl 8.18.0: a blackholed address and a listener that accepts TCP then stalls both return
  `http=000` and exit 28, so only `time_connect` separates a blocked port from a reachable one.
- Verify probes that targeted a literal reserved example address now reject an unsubstituted value
  locally (#50). Of 27 occurrences, 17 lines were probes whose pass signal is "nothing answered",
  where an unreplaced address times out exactly like a blocked port; the rest are configuration,
  certificate SANs, an ssh target and prose, where a wrong value fails visibly. Five QA rounds, and
  rounds 1, 3 and 4 each found a defect in the fix rather than in the original row: the placeholder
  alone traded one silent false pass for another, the guard warned and then ran the probe anyway,
  and under `set -u` a partial paste crashed in a way these steps call a refusal. `README.md`'s own
  checklist had the defect in its worst form, targeting `example.com`, which resolves and serves
  HTTPS, so its TLS check passed for every reader whatever state their deployment was in.
- Placeholder guards now unset their variable before assigning it, and five guides classify a local
  failure as inconclusive rather than a pass (#57). A pre-existing `declare -i` made the assignment
  arithmetic and silently set `0`, so the probe ran against nothing; `declare -l` lowercased the
  placeholder past the rejection pattern; and a stale exported value survived a paste that dropped
  the assignment. Separately, OpenBSD netcat returns exit 1 with no output at all when socket
  creation is denied, and BusyBox netcat rejects `-v` the same way, so "must fail to connect"
  accepted a check that never reached the network.

### Changed

- `VERSION` to 1.0.45.
- Maintainer rulings moved out of `TODO.md` into `DECISIONS.md`, and `PENDING-DECISIONS.md` added
  for questions awaiting a ruling during unattended operation (#51), in preparation for the OPF
  operational-files migration. Each ruling records the reasoning behind it, and a profile
  generalizes across them so fewer questions need asking; the profile is the orchestrator's
  inference, is marked fallible, and records where it has already been wrong. That change also
  corrected six rows whose ids used the priority band instead of the subject series, and restored
  the TLS-bypass gate's scope after a consistency edit had wrongly excluded the records files
  from it.
- `CONTRIBUTING.md` gains a "Shipping a change" section (#55): open the pull request, write its
  number into `VERSION`, push, then merge; merge in numeric order, and where that is not possible
  the later pull request sets the value; and record the change in `CHANGELOG.md` in the same pull
  request rather than a later one. The ordering rule exists because `VERSION` read literally makes
  the number go backwards when an older pull request merges last, which #50 did against a `main`
  already at 1.0.51.
- `CONTRIBUTING.md` rules 1, 5 and 6 (#60). Rule 1 gains a source-authority clause: for whether a
  control exists at all, the vendor's reference or CLI page is the authority, and absence from a
  quickstart is not evidence of absence. This repository had recorded Text Embeddings Inference as
  having no documented inbound authentication on the strength of its quick tour. Rule 5 gains an
  allowance for a check that cannot practically be run against the exposed state, which must then be
  marked at the step as reasoned rather than demonstrated; an unmarked step still claims a
  demonstration, so omitting the mark is itself a breach. Rule 6's prose had contradicted its own
  example ever since the guard became positional, and now gives the real reason for that form.
- Rule 5's reasoned-check allowance, narrowed the same day it was added (#60). Two review families
  independently reported that the first wording let an author mark anything reasoned. A reasoned
  step must now name the specific prerequisite that was unavailable, give the concrete command,
  state the expected exposed and fixed outcomes, and cite the vendor passage that distinguishes
  them; every locally feasible part is still run; a container on ordinary hardware is not
  impractical to stand up; and each reasoned mark opens a backlog row, because reasoned is a debt,
  not a destination.

### Records

- Backlog rotation and version bookkeeping: #46 moved the eight closed band 1 rows into `DONE.md`
  and opened three new band 1 rows and three new band 2 rows; #47 pinned `VERSION` to its own pull
  request number, correcting the stale value #46 shipped. #48 completed the pull-request references
  across every dated section and set `VERSION` to its own number, which is the authoring step row
  3.9 names.
- Backlog judgement and a second `VERSION` gap (#52). Row 1.38 asked for a per-guide judgement of
  46 filtered listener checks; all 46 were judged against the maintainer's ruling and none changed,
  because none claims that anything else is unexposed, 45 carry a trailing expectation comment, and
  the one without states its expectation in the prose below its block. Row 3.9 gained a second gap
  found while merging out of numeric order: the scheme names the most recently merged pull request,
  so when pull requests merge out of order the value goes backwards and no gate enforces
  monotonicity.
- Row 3.10 rotated to `DONE.md` (#54) now that the changelog-coverage gate it asked for is merged
  and green on `main`.
- The maintainer's four rulings of 2026-09-13 recorded in `DECISIONS.md`, and the two `.aiqt` local
  patches marked upstreamed in the PIN (#56). Guardrails accepted the `AIQT_SITE_HOST` change and is
  applying it as their own rather than taking a cross-repo pull request; preparing the details
  surfaced two problems in our own patches, an accidental lowercasing inconsistency between the two
  and an empty-value case that would make every dotted host resolve internal, and both were adopted.
  The patches drop when we re-pin to their first digest-verifiable release, which is the release row
  3.7 waits on.
- Four more rulings recorded, and two backlog rows corrected against their sources (#58). Row 2.15
  said Onyx has "complex default exposure"; the vendor documents authentication on by default since
  v4.4.0, `AUTH_TYPE` inert, and the real exposure being which compose file is run, since the
  production one publishes only nginx while the development one publishes seven services past the
  login page. That is the third of six row premises this verification pass overturned, after
  LangServe's deprecation and Mem0 shipping authentication on. Row 1.8 loses LocalAI to the new
  guide and records why, and row 1.43 opens for the published-backing-store pattern now seen in
  Dify, Mem0 and Onyx alike.
- Four more rulings recorded, and a second correction to the decision profile (#59). The
  permission-rule ruling is reversed on my own report that the evidence weakened; the
  AI-infrastructure guide gets two independent drafts rather than direct authoring; untested Verify
  checks ship marked as reasoned rather than demonstrated; and bands 2 and 3 stay ordered by the
  severity already recorded on each row. The profile now records two occasions in one day where a
  recommendation was overruled toward more rigour, so that pattern is evidenced rather than
  inferred.
- Row 3.9 rotated to `DONE.md` (#61). Its work shipped in #55 and has been live on `main` since; only
  the row was outstanding, and it was pulled out of a larger pending change at the maintainer's
  request so the backlog stops showing it as open.

## 2026-09-12

### Added

- `TODO.md` and `DONE.md` at the repository root (#38, #41). The backlog lived only in a private
  working store, so a question about it could not be answered without a summary, and writing it
  down exposed that the September audit's eighteen ranked coverage gaps were never recorded: five
  had shipped and thirteen are unrecoverable. A three-family audit replaced them, with codex and
  claude reading all 85 guides and gemini 16, producing sixteen gap rows and thirty enhancement
  rows, each naming what specifically to do. `DONE.md` records two terminal states, done and
  dropped, because a declined item that simply disappears gets proposed again.
- An Agent Plugin package (#39). `plugin.json` and one skill validated against the Agent Plugins
  and Agent Skills specifications, with the 86 guides bundled as `references/` so an adopter with
  no egress still has them, and `scripts/update-guides.sh` to refresh them and re-pin the version
  when there is network. The updater verifies every fetched guide against a published digest and
  replaces the bundle only once all of them match, so an interrupted run leaves the working bundle
  intact. Recording the bundle exposed two defects in the generated-file record gate: `kind` sat
  unused so a generated directory could not be recorded at all, and the build script did not
  declare the script it calls.
- `connection-poolers.md`, covering PgBouncer and pgpool-II (#30). A pooler becomes the thing
  clients connect to, so the database's own `hostssl` rules and TLS settings stop governing the
  client and start governing the pooler. Two PgBouncer defaults fail open: `client_tls_sslmode`
  is documented as "TLS connections are disabled by default", so a carefully TLS'd database
  gains nothing once a pooler fronts it, and `server_tls_sslmode` defaults to `prefer`, which
  "If refused, the connection will be established over plain TCP" and validates no certificate;
  `require` and `verify-ca` each close only half of that. A forced `user=` in a `[databases]`
  entry makes every client the same PostgreSQL role, so every `GRANT` and every per-user
  `pg_hba.conf` line stops discriminating. `exposure-index.md` gained 6432, 9898 and 9999.
- A shell-block gate (#27). Every fenced bash block is linted by shellcheck and parsed by
  `bash -n`, with the version reported rather than pinned, and a canary block whose finding has
  to come back: a reviewer silenced the real shellcheck through an environment variable so that
  it exited 1 with empty output, and the gate had read that as 156 blocks passing. It found five
  real defects in the corpus on its first run. A placeholder check shipped alongside it was
  deleted after five successive rules were each beaten by legal shell, the last by an ordinary
  `sed` substitution; CONTRIBUTING rule 6 now names that hazard as a review obligation and says
  plainly that no gate catches it reliably.
- A category gate covering `site/llms.txt` (#36). The README's guide index, the site menu and
  `llms.txt` are three hand-maintained copies of one category list, and nothing compared the
  third: it carried ten sections of its own against the README's twelve. The gate now checks all
  three, and checks `llms.txt`'s guide membership per category as well as its headings.
- A control-plane section in `kubernetes.md` (#23). A managed cluster's API server is reachable from
  the internet the moment the cluster is created, so a control plane nobody exposed on purpose is
  exposed; a kubeconfig may be a credential in itself or only a pointer to one, which decides what
  its disclosure costs; a self-managed control plane answers on 6443, 2379, 2380, 10250, 10255,
  10256, 10257 and 10259; the kubelet's flag and file defaults are opposites, so a reader who checks
  one learns nothing about the other; and GKE's DNS-based endpoint is not governed by authorized
  networks, so an allowlist that looks complete does not cover it. `exposure-index.md` gained rows
  for those ports.
- `self-hosted-idp.md`, covering Keycloak and authentik (#24). `hostname-admin` does not restrict
  the Administration REST API, so an operator who sets it can believe the administrative surface is
  closed while it answers; the administrative realm's login paths have to be restricted by source
  rather than refused; authentik's Compose install has a first-boot window in which whoever loads
  the page first becomes the administrator; and the management and metrics ports answer without
  asking who you are.
- `tools/check_gensrc.py` and `tools/test_gensrc_gate.py`, which gate `.aiqt/gensrc.json` against
  the build script it describes (#25). A new guide touches five wiring surfaces, and this was the
  fifth and the only one nothing checked, so the record of what generates `site/llms-full.txt`
  could fall out of step with the script that generates it while every other surface stayed green.


### Changed

- The backlog is banded and ordered by AIQT (#41, #42). Errors are band 1 and worked to empty
  before anything else, because a reader acting on a wrong guide is worse off than a reader with
  no guide; eight of the thirty rows against existing guides turned out to be errors rather than
  gaps. Band ids are decoupled from bands, so the reordering changed no identities.
- `site/_headers` pins the inline stylesheet by hash and drops `'unsafe-inline'` from
  `style-src`, with a gate that keeps the pin honest (#28). The gate refuses what it cannot
  model rather than modelling HTML: exactly one bare `<style>` and one bare `<script>`, every
  literal opener accounted for, no script-data escape states, no NUL byte, and one declared
  charset. Four review rounds of parser detail preceded that decision, and the round that
  settled it found the gate handing an author the hash of the empty string for a self-closing
  `<style/>` and going green once it was pinned.
- The weekly citation sweep reports where a cited page has moved, and fails the run when it has
  (#29). The link sweep accepts 301 and 302, so a moved citation passes it forever; #16 and #17
  were spent on that drift once it had spread across 82 citations. A green scheduled run
  notifies nobody, so a host or path move now reds the weekly workflow, which is not a required
  check and blocks no merge.
- `site/llms.txt` uses the README guide index's twelve categories, in the same order, with the
  same guides in each (#36). Seventeen entries that carried no description now have one, and two
  guides that were listed twice are listed once.

### Fixed

- `cors.md` and `host.md` cite the tools whose syntax they show (#35). `cors.md` demonstrated
  Express and FastAPI middleware while citing only MDN's protocol guide, and `host.md` showed five
  `ufw` and `firewall-cmd` invocations and cited neither tool, so rule 1 could not be exercised on
  either. A sweep for the same shape found ten more candidates, all false positives: this corpus's
  convention is for a source line to name the settings it covers, and they do.
- The DevProcess / OPF adoption is deferred and the reason recorded (#40). Its tooling ships in a
  later release, so adopting by hand would mean maintaining a TOML store and rendering its views
  by hand with no validator to catch drift between them, which is the defect class this repository
  already runs three freshness gates against.
- `chat-uis.md` no longer reproduces Chainlit's own login example (#31). The vendor's snippet
  compares a literal `"admin"` against a literal `"admin"`; the guide had swapped the literal
  for a house placeholder and kept both real defects underneath, a credential in application
  source compared with `==`. It reads both values from the environment and compares them with
  `hmac.compare_digest`, and the paragraph beside it no longer claims constant time the function
  does not promise, nor that rotating the password ends existing sessions, which
  `CHAINLIT_AUTH_SECRET` does.
- `kubernetes.md` no longer passes a password as a command-line argument (#33). Apache's own
  page says of `htpasswd -b` that "the password is clearly visible on the command line. For
  script use see the -i option", and the guide had copied Envoy Gateway's example. `-s` stays,
  because Envoy Gateway's basic auth documentation says "only SHA hash algorithm is supported
  for now", and the guide now says that rather than leaving SHA-1 looking like a free choice.
  `secrets.md` gained the rule that was missing, with the reason a probe for it is not offered:
  `grep` for the words password, token and secret finds only commands that spell them out, and
  a check that reads clean while the exposure is running is worse than none.
- `cloudflare.md` no longer publishes an application before authenticating it (#34). The guide
  said "The app is now reachable" at the end of step 2 and added Access in step 4, so a reader
  following the numbered order had a public, unauthenticated application in between, which
  `deployment-lifecycle.md` already warns against.
- `apache.md` and `lighttpd.md` probe more than the front door (#32). Four of the six
  fronting-proxy guides had been fixed and these two were missed, because neither proxies to an
  origin so the check the others use did not transfer. Apache's bypass is documented: an
  unmatched name falls through to "the first listed virtual host that matches" the address and
  port. Over TLS that name is the SNI one, so the probe sets both it and the Host header.
- `cors.md` and `host.md` cite the tools whose syntax they show (#35). `cors.md` showed Express
  and FastAPI middleware while citing only MDN's protocol guide, and `host.md` showed five
  `ufw` and `firewall-cmd` invocations and cited neither tool, so rule 1 could not be exercised
  on either.
- `kubernetes.md` and `memcached.md` gained Verify steps that discriminate (#26), and the
  changelog was brought up to date through #25.

### Records

- Changelog and version bookkeeping: #37 recorded #26 through #36 and set the version to match;
  #43 did the same for #35 and #38 through #42; #44 rotated rows 3.4 and 3.5 into `DONE.md`.

## 2026-09-11

### Added

- Two gates for conventions this repository stated and never checked (#21).
  `tools/check_verify_safety.py` fails when a command inside a Verify block skips TLS certificate
  verification, a rule stated in `common-mistakes.md`, `self-signed.md` and `README.sources.md` and
  broken in three Verify blocks before anything checked. `tools/check_prose_conventions.py` fails on
  British `-ise` spellings and on placeholders outside the house set. Both found defects live on `main`
  the first time they ran: `ray.md` carried `randomised`, which the corpus-wide conversion in #18 missed
  because its word list omitted that stem, and `pocketbase.md` carried `yourdomain.com`, a real
  registered domain that an audit had identified and no change had fixed.
- Three Verify steps that asserted a certificate check they did not perform (#21). `kafka.md`,
  `neo4j.md` and `memcached.md` each ran a bare `openssl s_client -connect` in a Verify block, two of
  them commented "TLS handshake with your certificate". Without `-CAfile`, `-verify_hostname` or
  `-verify_return_error` the handshake succeeds against any certificate, so the comment asserted what
  the command did not check, and `self-signed.md` and `rabbitmq.md` already carried the correct form.
  These surfaced only because the first version of the new gate got the openssl case backwards: it
  matched `-verify_return_error 0`, a syntax OpenSSL does not have, for a flag whose presence is the
  safe state. Cross-family review found the dead pattern, and fixing it exposed the guides behind it.
  The exemption for certificate inspection was removed after review showed it was a laundering pipe,
  since appending `| openssl x509` to an unverified handshake made the line pass, and the two guides
  that inspect a publicly trusted certificate, `deployment-lifecycle.md` and `free-certificates.md`,
  now pass because their commands carry `-verify_hostname` and `-verify_return_error`, not because an
  exemption covers them.
- `tools/test_convention_gates.py`, the regression suite for the two new gates (#21). It records one case
  per input a reviewer demonstrated against them, so every way past a gate that review found is a
  standing check rather than a one-off fix, and the inputs the gates still get wrong are asserted as the
  gates' current answer rather than dropped: quote state is per physical line, so a quotation spanning a
  line break hides what follows it, and inside a fence only a shell-style trailing comment is read as
  prose, so a hash inside a Python triple-quoted string is read as one. Both limits are named in the
  gates' docstrings as well.
- A "Bound the expensive endpoints" section in each of the four proxy guides (#20).
  `realtime-webhooks.md` and `authentication.md` both require request-size, concurrency and timeout
  limits on inference, upload and job-submission endpoints, naming denial of wallet as the failure
  mode, and `realtime-webhooks.md` recorded that the proxy guides did not carry those directives.
  They do now, and that sentence is rewritten to state what each proxy actually enforces rather than
  implying parity: nginx carries all four controls, Traefik body size, concurrency and rate but no
  timeout, HAProxy timeouts with an aggregate connection cap and a Content-Length body check, and
  Caddy body size only, with no rate limiting in its standard build. Two of those are CONTRIBUTING
  rule 3 statements rather than directives, since the control does not exist to configure. The change
  took four cross-family review rounds, three of them returning DO NOT SHIP from both families: the
  directives were correct throughout, while the snippets and the verification claims were not. Two
  snippets would have dropped a security directive if pasted over an existing block, so every snippet
  is now an explicit fragment, and the Verify steps report what they observe rather than asserting
  which limiter fired, because a backend returning the same status explains the result with no proxy
  limit present.
- `exposure-index.md`, a lookup from an observed listening port to the guides worth reading (#19). The
  corpus routes from a known mistake to its fix in `common-mistakes.md`; this routes from an observed
  symptom, which is the direction a reader arrives from. It claims deliberately little. A port does not
  identify a service here: 23 guides mention 3000, 21 mention 443, and six mention 8443, so the table says
  what may be listening and sends the reader to the owning process. Its Verify section is stated as a
  baseline inventory that cannot establish completeness, because a container on a bridge network
  publishes no host port and one on routed IPv6 answers on its own address whatever the host publishes.
  It took four cross-family review rounds: the first two returned DO NOT SHIP from both families, one
  for the false premise and one for a Verify section that certified an exposed application through an
  outbound tunnel and through endpoints no linked guide enumerates.
- A gate, `tools/check_guide_shape.py`, requiring every guide to carry a Verify section with a
  non-empty body and a `Sources (checked <month year>)` heading whose date names a real month, is not
  in the future, and cites at least one absolute URL with a hostname. It is deterministic and offline
  like the rest of the suite, and monotonic in time: its only date comparison can turn a failing guide
  into a passing one as the clock advances, never the reverse, so no build can go red from the
  calendar alone. The gate covers only the structural floor of `CONTRIBUTING.md` rule 5: it cannot
  prove that a Verify body holds a runnable command, that a Verify step fails while the service is
  still exposed, or that a cited page contains the line it is cited for, and it says so rather than
  implying wider coverage. `common-mistakes.md` is the one documented exemption. (#9)
- `README.sources.md`, holding the citations for the README verification checklist. The checklist
  names tools, status codes and services without citing them inline, so the sources now sit in a
  companion file and the front page stays readable. `README.md` is held to the same Verify and Sources
  contract as any guide, resolving its Sources through that file. Nothing mechanically ties a numbered
  check to its citation, so that correspondence is kept by reading. (#9)
- A fifth copy-ready prompt on the site that audits the AI and agent stack. (#6)
- A gate in `tools/run_all_checks.sh` that fails if the README guide-index category headings and the
  site menu category headings drift apart. (#6)

### Changed

- CONTRIBUTING rule 2 now states the Oxford `-ize` convention and the RFC 2606 placeholder set
  (#21). `tools/check_prose_conventions.py` enforces both, and rule 2 stated neither, so the gate
  was citing a rule as its source for something that rule did not say.

- Prose now uses Oxford `-ize` spellings throughout (#18). The corpus mixed British `-ise` and Oxford
  `-ize`, with the `-ise` forms holding the recurring line "login is not authorisation" in `README.md`,
  `authentication.md`, the `oidc-integration.md` heading and the site's front page. 27 instances were
  converted across 12 files. Technical identifiers were already `-ize` and are untouched: the
  `Authorization` header, `authorization_endpoint`, `authorizationEnabled`, AWS `authorizer`, and the
  term of art "authorization code flow". The case worth naming is `oidc-integration.md` lines 24 and 35,
  where prose `organisation` sat on the same line as the Microsoft Entra authority name `organizations`
  in backticks; both lines now carry the converted prose word and the identifier unchanged.

- The project was renamed from `sslconfig` to `secureconfig`. The GitHub repository is now
  `jposluns/secureconfig` and the site is served at `secureconfig.ai`; GitHub redirects the old
  repository path. Every
  absolute repository and site URL in `site/index.html`, `site/llms.txt`, `site/robots.txt`,
  `README.md` and `scripts/build-llms-full.sh` was updated, along with the `AIQT_SITE_HOST` gate
  variable in `tools/run_all_checks.sh` and the patch notes in `.aiqt/PIN`; `site/llms-full.txt` was
  regenerated. Earlier entries in this changelog keep the old name: they record what shipped under
  it. (#8)

- The `sslconfig.ai` domain was retired. Its DNS record was removed on 2026-09-11, so the old
  hostname no longer resolves and `secureconfig.ai` is now the only site hostname. GitHub continues
  to redirect the old `jposluns/sslconfig` repository path, but there is no redirect for the old
  domain: URLs in the wild that point at `sslconfig.ai` fail to resolve rather than forwarding. (#9)

- Three items in the README verification checklist were rewritten so that they discriminate. Each
  could previously be recorded as verified while the exposure it exists to catch was still standing.
  Item 4 now asks the reader to establish whether a failed `openssl s_client -tls1_1` came from the
  server or from a local client that never offered TLS 1.1, since the two are indistinguishable from
  the error alone. Item 7 now requires confirming that something is scheduled to invoke
  `certbot renew`, because a passing dry run proves the command works and nothing about whether it
  will ever be run. Item 10 no longer accepts a Shodan or Censys lookup in place of a live probe from
  a second host, because those services report what they last observed rather than what is listening
  now. (#10)

- The "Jump to" line was removed from the top of `site/index.html`. The left-hand menu and the
  in-page anchors it pointed at are unchanged. (#9)

- The README and the site front page (`site/index.html`) were realigned with the current corpus.
  One canonical description now appears in the README title, the site tagline, and both the meta and
  Open Graph descriptions. The AI-assistant rules, the decision guide, and the verification checklist
  were rewritten to cover identity and the allowlist, machine credentials, secrets, egress and
  metadata, exposed files, and the AI stack, and the README and site rule lists were reconciled. The
  flat guide index and the site menu were regrouped into the same twelve categories. No guide content
  changed. (#6)

### Fixed

- Seven accuracy defects, each re-checked against the vendor's own page or source before the edit, found
  by a tri-family QA scan of the whole corpus (#12). `model-servers.md` recommended a vLLM API key and
  verified only `/v1/models`; vLLM authenticates only the `/v1`, `/v2` and `/inference` prefixes and
  leaves `/invocations` open to the same inference capability, so a reader whose Verify step passed still
  had an unauthenticated inference endpoint. `workflow-orchestrators.md` named `[fab] auth_backends` to
  select Airflow's auth manager, which is `[core] auth_manager`; the FAB setting selects API
  authentication backends and is independent of it. `llm-observability.md` called the all-interfaces
  listener the OpenTelemetry Collector default, which has been `localhost` since v0.110.0.
  `nextjs.md` and `paas.md` still required Pro or Enterprise to protect a Vercel production domain,
  contradicting `cloud-identity-proxies.md`, which already carried the 9 September 2026 change making it
  free on every plan.
- Four Verify steps that could not discriminate (#12). The Prefect check expected a 401 on `/api/health`,
  which the server exempts on GET so container probes keep working, so it would have failed a correctly
  configured server; it now probes `POST /api/flows/filter`. The Langfuse check treated
  `/api/public/health` as an access-control test although it returns health status by design. The MinIO
  check tested the service root, which does not prove any bucket is private, because anonymous policies
  are set per bucket. The OTLP probe sent `{}` with curl's default form encoding, which the Collector
  rejects with 415 on content type before reaching authentication, so the rejection proved nothing.
- Nine Verify steps that could pass, or fail, for reasons unrelated to what they claim to prove (#13).
  `container-hardening.md` spliced the database address into a `kubectl --overrides` JSON array unquoted,
  so the override was not valid JSON, kubectl rejected it, and the guide's only proof that the default-deny
  NetworkPolicy blocks anything never ran; the same override named its container `probe` while `kubectl
  run` names the container after the pod, so a strategic merge would have added a container rather than
  replaced the command. Its read-only filesystem probe wrote to `/x`, which the configured non-root user
  cannot create on a writable filesystem either. `web-exposure.md` sent grep's errors to `/dev/null` and
  read empty output as clean, although grep exits 2 and prints nothing against a directory that does not
  exist, so a reader whose framework built elsewhere was told an unscanned bundle was clean; its ACME
  check also forbade a 404 on a challenge token it never created, which a correctly exempted directory
  returns anyway. `devops-uis.md` tested Docker client certificates with a command that does not enable
  TLS and could inherit a certificate from an export earlier in the same guide. `n8n.md` and `php.md`
  inferred route protection from a HEAD response and from cookie flags.
- `egress-metadata.md` verified the metadata services' header requirement rather than the network block
  the guide itself recommends as the control (#13). Added a probe of the block, sending the header the
  service requires, which must time out or be refused.
- Three Verify steps that disabled TLS certificate verification, which the corpus forbids in
  `common-mistakes.md`, `self-signed.md` and `README.sources.md` (#14). `elasticsearch.md` sent
  credentials over a connection whose certificate was never checked, and claimed in a comment that
  plaintext does not answer while making no plaintext request; `jupyter.md` and `devops-uis.md` probed
  with `curl -skI`. A `-k` probe passes against a substituted certificate as readily as against the real
  one, so the TLS half of each check proved nothing. All three now verify, and the plaintext refusal is
  its own check.
- A citation in `elasticsearch.md` that still returned 200 but redirected to a generic install page
  carrying no security configuration (#14). The weekly link sweep treats a redirect as passing, so a
  stale citation of this shape stays green indefinitely. Replaced with the current documentation home,
  and the page documenting the generated `http_ca.crt` is now cited beside it.
- The four fronting-proxy guides never checked the backend's bind address (#15).
  `common-mistakes.md` opens the corpus-wide list with "Binding to `0.0.0.0` to fix a connection
  problem and never binding back", and `nginx.md` repeats it as its own first common mistake, yet
  the Verify blocks in `nginx.md`, `caddy.md`, `haproxy.md` and `traefik.md` probed only the front
  door. All of those checks pass while the application also answers directly on port 3000, bypassing
  the proxy's TLS and authentication, so a reader in exactly the state this project ranks first
  collected four passes. Each Verify block now ends with the `ss -tlnp` check already used in
  `model-servers.md`, `vector-databases.md`, `docker.md` and the database guides; `traefik.md` also
  checks published container ports. `apache.md` and `lighttpd.md` serve content directly and front
  no backend, so the check does not apply to them and they were left alone.
- 82 citations that had silently moved (#16). An audit of all 767 URLs cited in the corpus found no
  broken links and 84 redirects to a different path. The weekly link sweep counts a redirect as
  passing, so a citation whose vendor reorganized its documentation stays green while pointing away
  from the syntax it supports. ClickHouse, Coolify, NATS, the GitHub Actions OIDC pages, Gateway API
  and Google Cloud all restructured; `encode/uvicorn` moved to `Kludex/uvicorn` and
  `kubernetes/dashboard` to `kubernetes-retired/dashboard`. Every replacement target was fetched and
  confirmed to resolve to itself, and where a redirect collapsed to a bare documentation root the
  real successor page was found instead. Two of the refreshed citations had been added earlier the
  same day, in #12 and #13. This also settles the disagreement between `README.md` and
  `README.sources.md` over the testssl.sh URL.
- Two URLs were deliberately left pointing at what looks like a stale target, because neither is a
  citation (#16). `cloud-identity-proxies.md` keeps `https://cloud.google.com/iap` as the JWT `iss`
  claim a reader validates Google IAP tokens against, and `egress-metadata.md` keeps
  `https://sts.amazonaws.com/` as the positive-control probe in its Verify block, which is the
  endpoint a workload calls for role credentials rather than a page to read.
- Four citations from #16 whose refreshed target stopped matching its own description (#17).
  `machine-auth.md` promised RFC 6749 section 4.4 while the new URL dropped the `#section-4.4` fragment,
  and now cites the IETF datatracker copy, which serves anchors. `python.md` promised a settings
  reference while the new URL landed on Gunicorn's home page; Gunicorn restructured its documentation and
  the reference is now at `/reference/settings/`, confirmed to carry `bind`, `certfile`, `keyfile` and
  `ca_certs`. Helicone and NATS each merged two pages into one, so `llm-observability.md` cited the same
  URL twice in a single bullet and `nats.md` cited one page from two bullets; both are now cited once.
  The detection gap worth recording: the audit flagged redirects that lost path depth, which catches a
  page collapsing to a documentation root, but not `docs.gunicorn.org/` redirecting to `gunicorn.org/`,
  where the depth is unchanged and only the host differs.
- The `rabbitmq.md` Verify step could not complete the mutually authenticated connection the guide's own
  configuration requires (#21). `ssl_options.fail_if_no_peer_cert = true` makes the broker demand a
  client certificate and the command supplied neither `-cert` nor `-key`, so the run that failed to
  connect still printed "Verification: OK", which reports the server certificate rather than the
  client's, and the comment named that line as the pass condition. It is now a pair: one invocation
  carrying client credentials that must reach a session, one without them that the broker must refuse,
  with the pass condition stated as the shape of the outcome rather than a message to match on. The
  negative run holds stdin open with `sleep 2` rather than closing it with `</dev/null`, because on TLS
  1.3 the server can only refuse an empty client certificate after the client's flight and s_client
  otherwise reaches end of input and exits before the rejection arrives; measured against a server
  requiring a client certificate, the `</dev/null` form exited 0 and printed a full session block in four
  trials of five, indistinguishable from the positive run.
- `deserialises` in `ray.md`, a second `-ise` spelling in the same file (#21). It sits in the opening
  risk statement, describing what cloudpickle does to arbitrary Python objects, and now reads
  `deserializes`.
- `kafka.md` named the unprefixed `ssl.client.auth=required` as the way to require client certificates
  (#21). The guide configures a SASL_SSL listener, and its own section 5 already states that the
  unprefixed setting applies to SSL listeners only and that Kafka logs a warning when it is set without
  the prefix on a SASL_SSL broker, so the advice was inert: a reader who followed it would have believed
  client certificates were required while they were not. It now names
  `listener.name.sasl_ssl.ssl.client.auth=required`, which is what the rest of the guide already says.
  The clause was added earlier in this branch and corrected within it.
- `ollama.md`'s bearer-token variant silently dropped two directives its `location /` variant
  carries (#22). `proxy_set_header Host localhost:11434` and `proxy_read_timeout 300s` are restored,
  so a reader who chose the authenticated variant gets the same upstream settings as the other one.
  The RunPod port wording in `gpu-clouds.md` was corrected in the same change.

### Records

- Changelog and version bookkeeping: #7 recorded the front-door realignment; #11 set the project
  version and completed the changelog for #9 and #10.

## 2026-09-10

### Added

- Nineteen guides from a three-family forward-looking gap review: `fronting-auth.md` (oauth2-proxy,
  Authelia, Pomerium wiring, which six existing guides already referenced), `web-exposure.md`,
  `egress-metadata.md`, `realtime-webhooks.md`, `container-hardening.md`, `deployment-lifecycle.md`,
  `image-gen-uis.md`, `chat-uis.md`, `llm-observability.md`, `workflow-orchestrators.md`,
  `gpu-clouds.md`, `nats.md`, `search-engines.md`, `bi-dashboards.md`, `pocketbase.md`,
  `frontend-frameworks.md`, `sqlite.md`, `tunnels.md`, and `surrealdb.md` (#5). Each cites vendor pages
  fetched in September 2026; unconfirmable details were left out.
- Sections folded into existing guides: a shared-cache disclosure section in `headers.md`, a
  fail-closed rule in `cloud-identity-proxies.md`, CAA records in `free-certificates.md`, an
  expensive-endpoint limit note and an offboarding rule in `authentication.md`, an offboarding check
  in `mfa.md`, Dozzle, Docker Registry, Filebrowser, and Node-RED in `devops-uis.md`, Hugging Face
  Spaces in `paas.md`, a Redpanda note in `kafka.md`, and a Valkey note in `redis.md` (#5).
- README gained an outside-in verification checklist item, a fronting-auth pointer in the
  authentication rule and decision guide, and index rows for every new guide; the site menu,
  `site/llms.txt`, the build script, and `.aiqt/gensrc.json` were updated to match (#5).
- Twenty-one guides: `identity-providers.md`, `oidc-integration.md`, `cloud-identity-proxies.md`,
  `machine-auth.md`, `nextjs.md`, `go.md`, `dotnet.md`, `java.md`, `php.md`, `ruby.md`, `kafka.md`,
  `clickhouse.md`, `neo4j.md`, `memcached.md`, `object-storage.md`, `vector-databases.md`,
  `mcp-servers.md`, `ray.md`, `mlflow.md`, `agent-builders.md`, and `devops-uis.md` (#4). Configuration
  syntax in each was checked against vendor documentation fetched in September 2026, and each
  guide's Sources section lists the pages. A three-family review (Claude, Codex, Gemini) of the
  whole branch then found and corrected further defects, listed under Fixed; details that no page
  confirmed were left out.
- `authentication.md` rules 11 to 15: federated login is not authorization, OIDC and OAuth hygiene,
  MFA enforced where access is granted, control-plane MFA, and authentication on every transport;
  plus a negative-test quick check (#4).
- `mfa.md`: a phishing-resistant factor for administrators, enrolment is not enforcement, a pointer
  to hosted providers, and a section on passkeys and hardware keys (#4).
- `common-mistakes.md` items 15 to 19, including federated login treated as authorization and MCP
  servers bound to all interfaces (#4).
- `model-servers.md` now covers Text Generation Inference, SGLang, Triton, and LM Studio (#4).
- README: an AI-assistant rule on federated login and a decision-guide entry for team and customer
  login (#4). Site: menu entries for every new guide, the same rule, and a fourth copy-ready prompt,
  "Add single sign-on and MFA".
- The wiring gate now also fails when a guide is missing from `README.md` or the site menu.

### Changed

- `kubernetes.md` rewritten on the Gateway API (Envoy Gateway, cert-manager Gateway support,
  SecurityPolicy for basic auth and OIDC) because the Kubernetes project retired ingress-nginx in
  March 2026 with no further releases or security patches. The guide tells readers how to detect
  ingress-nginx and that migration is required.
- `free-certificates.md` carries Let's Encrypt's announced lifetime schedule (64-day certificates
  from 2027-02-10, 45-day from 2028-02-16, an industry cap of 47 days from 2029-03-15) and notes
  that Let's Encrypt ended OCSP on 2025-08-06, so no stapling directives for its certificates.
- `paas.md` distinguishes platform deployment protection (Vercel) from application user
  authentication instead of saying the platform authenticates nobody.
- `redis.md` describes mutual TLS as a possession factor for machine clients, not MFA for a person.
- `linkcheck.yml` documents why 403, 429, and 999 are accepted: a green sweep proves every URL
  answered, not that every page was readable.

### Fixed

- `tools/check_site.py` reads the site host from `AIQT_SITE_HOST`, so absolute `sslconfig.ai`
  self-links in `site/index.html` are resolved rather than skipped as external. Recorded as a local
  patch in `.aiqt/PIN`.
- `CLAUDE.md` and `AGENTS.md` now describe the wiring gate's real coverage, and `.aiqt/gensrc.json`
  lists every source of `site/llms-full.txt`.
- Pre-existing guide defects found by the review: Redis inline comments that its parser rejects;
  PostgreSQL `listen_addresses` needing a restart rather than a reload; MongoDB client examples
  lacking the certificate the server configuration demanded; Caddy `/admin/*` not matching
  `/admin`; lighttpd `mod_redirect` not loaded; SSH MFA advice that conflicted with
  `KbdInteractiveAuthentication no`; Open WebUI's persisted `ENABLE_SIGNUP`; the Streamlit login
  example admitting any Google account; the multi-port `nc` check in `cloud-firewalls.md`; and
  several inline comments inside properties and INI values in the new guides.
- A gate that recomputes the sha256 of every inline script in `site/index.html` and requires it in
  the effective CSP line of `site/_headers`; the wiring gate now matches real index rows and menu
  links rather than any text; same-page anchors are validated.

## 2026-09-09

### Added

- An offline gate suite, `tools/run_all_checks.sh`, run on every pull request and on every push to
  `main` as the check `gates`. Four checks to start: `site/llms-full.txt` matches a fresh build,
  every guide is listed in the build script and linked from `site/llms.txt`, every local link target
  and heading anchor resolves, and no private keys or provider tokens appear in tracked files. Every
  check is deterministic and offline, so nothing outside this repository can turn the build red.
  (#1)
- An AIQT Guardrails baseline, vendored and pinned to guardrails
  `8ab5b9ae37cdf4f8bcdb0a73934cb5a58b2aea87`. `CLAUDE.md` and a byte-identical `AGENTS.md` carry the
  AIQT priority ordering and the five rules. The pack's hooks are vendored under `.aiqt/` but are
  deliberately not activated. Three upstream gates joined the suite: `check_site`, `check_no_dashes`,
  and `check_newtab`. The pin and the local patches to re-apply on upgrade are recorded in
  `.aiqt/PIN`. (#2)

### Changed

- Every external link in `site/index.html` now opens in a new tab with `rel="noopener noreferrer"`.
  Fifty-five links changed; the four `sslconfig.ai` links are internal and were left alone. (#2)

### Fixed

- The weekly link check had failed since 2026-09-07 because lychee cannot resolve the root-relative
  `/favicon.svg` in `site/index.html` without a root directory. Passing `--root-dir` fixes it, and
  the sweep now reports 555 of 555 links good. (#1)

### Repository settings

- `main` is protected: a pull request is required, force-push and deletion are blocked, and the
  `gates` check must pass before a merge.
- Issues and pull requests were both disabled on the repository and are now enabled. Pull requests
  being disabled is why the API rejected every attempt to open one, reporting it misleadingly as a
  personal-access-token permission error.

### Records

- #3 added this changelog and exempted it from the guide-coverage gate.
