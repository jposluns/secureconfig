# TODO

Forward-looking backlog for secureconfig. Closed items move to `DONE.md`; nothing is deleted.

This file is not a guide. It carries no configuration and is excluded from the guide-shape and
site-wiring gates for that reason; see `not_a_guide()` in `tools/run_all_checks.sh`.

## How items are numbered

Every open item is one index row in the band below that fits it. **Ids are permanent and never
reused**, including when an item is dropped, superseded, or its work reverted, and they are
decoupled from the band so an item can move band without changing identity.

Series: **1.x** an existing guide, whether the row is an error or a deepening, **2.x** missing
content, **3.x** tooling and process, **4.x** the site and adopter-facing surfaces. A row moves
band without changing its id, which is the point of decoupling the two.

Each row carries `(severity, effort)`. Severity is what a reader loses: **H** a wrong or missing
control on something commonly exposed, **M** a real gap with a workaround, **L** cosmetic or
internal. Effort is **XS** minutes, **S** under an hour, **M** a session, **L** several sessions,
**XL** a project.

Next ids: **1.86**, **2.40**, **3.16**, **4.12**.

Retired without ever naming an item, and never to be issued: **2.21** to **2.23** and **4.3** to **4.4**, assigned in error on 2026-09-13 when the band number was used in place of the series.

## Queueing

**AIQT: fix issues first.** Band 1 is worked to empty before anything else is picked, whatever
the severity of what is waiting in the other bands. After band 1 is clear, take the highest
severity across bands 2 and 3 together rather than finishing a band.

Maintainer direction supersedes the order at any time. Items blocked on another project are not
picked, they are waited on.

## Priority 1: Errors

A guide that asserts something false, or a Verify step that cannot discriminate. These are worked
first and to empty, because a reader acting on a wrong guide is worse off than a reader with no
guide. Found by the 2026-09-13 coverage audit; `[N families]` means raised independently by that
many.

| ID | Item | Tags |
| --- | --- | --- |

## Priority 2: Deepen existing guides

A real surface the guide never covers. Correct as far as it goes, and not far enough.

| ID | Item | Tags |
| --- | --- | --- |
| 2.24 | Demonstrate `ai-infra-services.md` Verify checks 2 to 7 against real deployments. They ship marked reasoned rather than demonstrated, because the authoring environment has no container runtime and so no live service, external vantage, TLS exchange, or browser flow. CONTRIBUTING rule 5 requires the debt to be tracked, not merely disclosed. Stand up the six services from their cited Compose files and run each check against the exposed state as well as the fixed one (M, L) | `[gap]` |
| 2.25 | Demonstrate the `headless-cms-instant-api.md` Verify probes against live Strapi, Directus, Hasura and PostgREST instances in both the exposed and fixed states. They ship marked reasoned (#126, reworked #211) because the authoring environment has no container runtime; stand up each from its vendor Compose, seed a `secureconfig-canary` record, and confirm the paired matched control (authorized `CMS_PROBE_HEADER` returns the canary; anonymous `CMS_NEGATIVE_HEADER` unset gives the denial) for each content-based discriminator (Strapi opened content type vs default denial, Directus item vs auth error, Hasura data vs the admin-secret-required body at HTTP 200, PostgREST anon-grant data vs `PGRST302`, the invalid-token `PGRST301`, and the Hasura console-disabled not-found), the default/leaked-credential re-test (removed Strapi token, replaced Directus `ADMIN_TOKEN`, copied Hasura secret, retired-key JWT, and a fresh-browser-session admin-password check), the write/registration/media/MFA/introspection/egress verifications, and the `ss` plus external-vantage listener check on 1337, 8055, 8080, 8081, 3000 and the optional PostgREST admin port (M, M) | `[gap]` |
| 2.26 | Demonstrate the `headless-browser-services.md` Verify probes against live instances in both the exposed and fixed states. They ship marked reasoned (#127) because the authoring environment has no container runtime; stand up a Chrome with `--remote-debugging-port` (confirm `/json/version` returns the `webSocketDebuggerUrl` when address-exposed and refuses on loopback), a Selenium Grid 4 (confirm `4444/status` and a Node `5555/status`, and that Router basic auth does not cover the Node or the 4442/4443 event bus), a browserless (confirm `/pressure` is open with no `TOKEN` and the exact rejection once set), and a Playwright `run-server` (confirm the ws port answer and pin the default `--host`/`--path`), plus the `ss` and external-vantage listener check on 9222, 4442, 4443, 4444, 5555, 3000, 5900 and 7900 (M, M) | `[gap]` |
| 2.27 | Demonstrate the `supabase-self-hosted.md` Verify probes against a live self-hosted Supabase stack in both the exposed and fixed states. They ship marked reasoned (#128, reworked #208) because the authoring environment has no container runtime; stand up the docker stack unedited, confirm the demo `ANON_KEY`/`SERVICE_ROLE_KEY` on `/auth/v1/settings` return settings JSON rather than a rejection (paired with a current key), the FUNCTIONS_VERIFY_JWT no-key/with-key function pair, the default dashboard basic-auth is accepted, and `psql` connects to the Supavisor pooler with the demo `POSTGRES_PASSWORD` running `SELECT current_user`; then rotate the secrets and keys and confirm each is refused, including a forged HS256 `service_role` JWT signed with the demo `JWT_SECRET` (the check that proves the secret itself was rotated, not just the keys swapped) and that every published port is unreachable externally (M, M) | `[gap]` |
| 2.28 | Demonstrate the `gitops-controllers.md` Verify probes against live Argo CD and Flux installs in both the exposed and fixed states. They ship marked reasoned (#129) because the authoring environment has no container runtime; stand up each from its pinned quickstart/bootstrap, confirm the Argo CD anonymous-API discriminator (application JSON vs `401`), the `argocd-initial-admin-secret` presence/absence, an unsigned vs signed webhook against a `generic` vs a `generic-hmac` receiver, and the `kubectl get svc`/`ss` listener inventory, recording pinned versions and rendered ports (M, M) | `[gap]` |
| 2.29 | Demonstrate the `self-hosted-ci-runners.md` Verify checks against a live registered ephemeral runner and an exposed persistent/privileged counterpart. They ship marked reasoned (#130, reworked #213) because the authoring environment has no container runtime, registered test runner, or cloud test infrastructure; register an ephemeral GitHub JIT runner and a GitLab autoscaler runner, and confirm the reworked checks discriminate exposed vs fixed: the GitHub runner-group/group-repository/repository-level-runner REST eligibility listings, the workflow-permission posture, the public-fork behavioral probe (the `runner-eligibility-probe` workflow assigned to a disposable runner must execute a fork PR while eligibility is exposed and must not after the private-repository restriction, checked via the workflow-jobs API), the `tomllib` config inspection (privileged/services_privileged/`host` tcp:// daemon/autoscaler one-job limits; runs offline against fixtures), the GitLab job-token/runner API scope and `ref_protected` access level, the two-job contamination canary, and the metrics/session-server reachability from a disallowed vantage (M, M) | `[gap]` |
| 2.30 | Demonstrate the `vault.md` Verify checks against a live Vault (pinned version): `vault status` and `sys/health` codes in the sealed and unsealed states, TLS positive and a plaintext-request negative, `vault audit list` plus a correlated audit-log entry for a test request, the initial-root token rejected after revocation with a non-root control request, and the unfiltered `ss` bind inventory of 8200/8201. Ships reasoned because the authoring environment has no container runtime (M, M) | `[gap]` |
| 2.31 | Demonstrate the `transactional-email-posture.md` Verify probes against a controlled sending domain in both the exposed and fixed states. They ship marked reasoned (#132) because the authoring environment has no DNS control, SMTP endpoint, or container runtime; publish the SPF, DKIM, DMARC (`p=reject`), MTA-STS and TLS-RPT records for a controlled domain and confirm each with `dig`, run `openssl s_client` against 587 and 465 to confirm TLS completes before AUTH and that a stripped or plaintext submission is refused, send a controlled message and read the receiver's trusted `Authentication-Results` for an aligned SPF or DKIM pass, and confirm open-relay rejection (an external `MAIL FROM` with a non-local `RCPT TO` draws a 5xx) in an operator-controlled MTA (M, M) | `[gap]` |
| 2.32 | Demonstrate the `time-series-metrics-stores.md` Verify probes against live InfluxDB (2.x and 1.x), VictoriaMetrics (single-node and cluster), and QuestDB in both the exposed and fixed states. They ship marked reasoned (#133) because the authoring environment has no container runtime; stand each up from its vendor image, confirm the discriminators (InfluxDB 2.x `/api/v2/setup` `allowed:true` to `false`, InfluxDB 1.x `/query` database JSON to 401, VictoriaMetrics `/api/v1/query` series to 401 behind `vmauth`, QuestDB `/exec` result to 401 and the `admin`/`quest` PGWire login refused after change), and the `ss` listener inventory of 8086, 8428, 8480, 8481, 8482, 9000, 8812, and 9009 (L, M) | `[gap]` |
| 2.33 | Demonstrate the `self-hosted-error-trackers.md` Verify probes against live Sentry (getsentry/self-hosted) and GlitchTip in both the exposed and fixed states. They ship marked reasoned (#134) because the authoring environment has no container runtime; stand each up from its vendor Compose, confirm the registration discriminator (Sentry redirects `/auth/register/` to the org login under single-org mode, so follow the redirect and confirm the login page presents a create-account path only when `auth.allow-registration` is on; GlitchTip self-signup accepted with `ENABLE_USER_REGISTRATION=True` and refused when `False`), that the web redirects to the HTTPS proxy rather than serving plaintext, and the `ss` inventory of 9000 and 8000 with no backing store (PostgreSQL, Redis, Kafka, ClickHouse) published beside them (L, M) | `[gap]` |
| 2.34 | Demonstrate the `workflow-orchestrators.md` Verify probes against live Prefect, Dagster, Airflow, Temporal, Flower, and Argo deployments in both the exposed and fixed states. They ship marked reasoned (#210, #218) because the authoring environment has no container runtime; stand each up from its vendor Compose or CLI, confirm the Prefect `/api/flows/filter` anonymous-vs-authorized JSON discriminator (an anonymous flow list, even empty, is the finding; health/ready stay exempt), the Dagster `/graphql` anonymous `repositoriesOrError` query returning data, the Airflow `/auth/token` accepting the Compose `airflow`/`airflow` account plus a no-token vs token `/api/v2/pools` read, the Temporal `temporal workflow list` frontend gRPC call answered with no claim (excluding health checks), and the Flower `/api/workers` anonymous worker list (401 under Basic auth, a provider redirect under OAuth), and the Argo `/api/v1/workflows/{namespace}` anonymous-vs-authorized list (an anonymous 200 list under server mode is the finding; client/sso give 401 without a token or 403 on an authorization denial), which needs a Kubernetes cluster and the Argo install manifests, each paired with a matched authorized control against the same origin and a direct-origin reachability check from an untrusted vantage (M, L) | `[gap]` |
| 2.35 | Demonstrate the `realtime-webhooks.md` Verify probes against live WebSocket, SSE, and webhook endpoints in both the exposed and fixed states. They ship marked reasoned (#212) because no endpoint, WebSocket client, or event fixtures were available; stand up an app exposing ws/sse endpoints and a signed-webhook receiver, seed `AUDIT_COOKIE`, `AUDIT_WEBHOOK_SECRET` (and `AUDIT_GITHUB_EVENT`), and confirm the ws/sse handshake prober's matched controls (101 with a valid cookie and allowed Origin versus a rejection or login redirect without it and for each invalid-Origin case; 200 `text/event-stream` versus a rejection for SSE), the reasoned first-message-token and default-credential cases, the GitHub/Stripe HMAC harness (wrong and unsigned rejected before the handler, the valid delivery processed once, the stale Stripe request rejected on timestamp), and the `--connect-to` direct-origin reachability from an external vantage (M, L) | `[gap]` |
| 2.36 | Demonstrate the `streamlit.md` Verify against a live Streamlit 1.64.0 in the exposed and fixed states. They ship marked reasoned (#214) because the audit environment has no Streamlit or container runtime and cannot create listening sockets; stand up an app both behind a fronting proxy and with native `st.login`, seed `st.write("SECURECONFIG_AUTH_CANARY")` after the gate, and confirm the TLS probe, the `/_stcore/health` no-auth discriminator (a 200 `ok` through the proxy is the finding; native `st.login` serves an anonymous 200 on `/` by design), the direct-origin `8501` external-reachability guarded probe, and the browser matched controls (anonymous denied, allowed-user-with-MFA sees the canary, an identity outside the allowlist denied, and with the boundary disabled the anonymous visit reveals the canary) including that an unauthenticated websocket upgrade is denied behind the proxy (M, M) | `[gap]` |
| 2.37 | Demonstrate the `sqlite.md` Verify against exposed and fixed deployments with matched positive controls. The listener, Turso-authentication, and LiteFS/Litestream-isolation checks ship marked reasoned (#215) because the authoring environment has no such service runtime, no external vantage, and cannot create listening sockets; stand up a web-served database, a Turso database, and a LiteFS/Litestream node, then confirm the web-download probe (the `.db` and `-wal`/`-shm` sidecars answer 404, never 200/206, with the positive control answering), the token-safe bundle scan, the git check-ignore/ls-files/log inventory, the `stat` directory-and-sidecar permission check, the LiteFS `20202` and Litestream metrics/MCP listener isolation from an untrusted vantage against an authorized positive control, and the Turso `/v2/pipeline` anonymous-vs-authorized discriminator (anonymous query success is exposure). Record deployed versions (M, M) | `[gap]` |
| 2.38 | Demonstrate the `search-engines.md` Verify against isolated exposed and protected Meilisearch and Typesense deployments with matched positive controls. They ship marked reasoned (#216) because the audit environment has no engine binary or container runtime; stand each up, seed a known fixture, and confirm through the paired-request block the authentication discriminators (Meilisearch keyless-vs-protected `POST /indexes/movies/search`; Typesense `401` not `403` on a key lacking the action), the write-restriction pairs (Meilisearch `202` task vs `403`; Typesense `201` vs `401`), tenant/scoped-key document and JOIN isolation with allowed/forbidden fixtures, the disclosed-key exposed-state discriminator and its post-rotation failure, and the `nc` direct-origin reachability of `7700`/`8108`/`8107` from an untrusted vantage against a live private control. Record engine versions, requests, and response bodies/statuses (M, M) | `[gap]` |
| 2.39 | Demonstrate the `sqlite-http-frontends.md` Verify against isolated exposed and fixed Datasette and sqlite-web deployments. Checks are REASONED because the authoring environment has neither service runtime nor a container runtime, denies socket creation and inspection, and has no authorized external vantage or valid deployment credentials. Record exact installed versions; demonstrate listener and container-publication inventory, direct-origin and provider-hostname isolation, authorized-positive-control versus anonymous UI/SQL/JSON access, authenticated-but-unauthorized rejection, disposable-database write rejection, disabled SQL with permitted browsing, disabled downloads, introspection-route protection, and CSRF behavior for enabled write plugins. Record requests, response bodies, and outcomes (M, M) | `[gap]` |
| 1.45 | `llm-observability.md`: demonstrate the reworked Verify checks against live Langfuse, Phoenix, Helicone, and an OpenTelemetry Collector in both the exposed and fixed states. They ship marked reasoned (#62, broadened #200) because the authoring environment has no such deployment: the Langfuse public-API no-key/with-key matched pair, the Collector OTLP/HTTP no-auth/with-auth pair, Phoenix's unauthenticated-read probe, the manual default-credential check, and the Phoenix OTLP/HTTP empty-protobuf admission probe plus the separate gRPC 4317 receiver all need running instances; `PHOENIX_DEFAULT_ADMIN_INITIAL_PASSWORD` first-startup semantics also want confirming against a live version (M, M) | `[gap]` |
| 1.77 | `clickhouse.md`: demonstrate the reworked Verify checks against a live ClickHouse in both the exposed and fixed states. They ship marked reasoned (#62) because the authoring environment has no ClickHouse server or container runtime: the disabled-plaintext-8123 transport-failure probe, the HTTPS-8443 empty/wrong/correct matched triple as `app` against the same origin, and the native-9440 wrong-then-correct discriminator all need a running server; the ClickHouse 26.2 native-TOTP behaviour and `interserver_http_credentials` enforcement also want confirming against a live version (M, M) | `[gap]` |
| 1.78 | `code-server.md`: demonstrate the reworked Verify checks against a live code-server in both the exposed and fixed states. They ship marked reasoned (#62) because the authoring environment has no running code-server: the unauthenticated editor-URL probe (login page vs editor), the authenticated positive control, the public-origin reachability probe, and the proxy-route (`/proxy`, `/absproxy`, `--proxy-domain`) authentication checks all need a running instance (M, M) | `[gap]` |
| 1.79 | `connection-poolers.md`: demonstrate the backend-certificate matched-control tests against a live PgBouncer/pgpool-II in an isolated deployment. Most of the Verify is runnable, but the untrusted-CA / wrong-hostname / backend-declines-TLS cases with forced-fresh-backend sequencing ship reasoned (#62) because the authoring environment has no pooler or PostgreSQL runtime; the direct-DB-bypass rejection and the PCP wrong-password refusal also want confirming live (M, M) | `[gap]` |
| 1.80 | `haproxy.md`: demonstrate the reworked Verify against a live HAProxy 3.0 in exposed and fixed states. They ship reasoned (#62) because the authoring environment has no HAProxy runtime: the HTTP-redirect check, the no/wrong/valid auth triple, the body-size under/over pair with the deny-line-removed attribution fixture, the stats-listener unauth/auth pair, the runtime-socket ownership/level check, and the mTLS no-cert/untrusted/trusted cases all need a running instance (M, M) | `[gap]` |
| 1.81 | `nats.md`: demonstrate the reworked Verify against a live NATS/JetStream in exposed and fixed states. They ship reasoned (#62) because the authoring environment has no NATS runtime: the authenticated publish/subscribe positive controls, the clean-env plaintext and TLS-no-credential negative controls, the forbidden-subscribe permissions-error check, the verify:true password-vs-mTLS separation, and the guarded 8222 monitoring probe all need a running server; the cluster/leafnode/gateway route-auth checks also want confirming live (M, M) | `[gap]` |
| 1.82 | `surrealdb.md`: demonstrate the reworked Verify against a live SurrealDB in exposed and fixed states. They ship reasoned (#62) because the authoring environment has no SurrealDB runtime: the paired `INFO FOR DB;` probe (unauthenticated rejection vs a valid system-user JWT), the origin-reachability probe, and the capabilities/SSRF and JWT issuer/audience behaviours all need a running server (M, M) | `[gap]` |
| 1.46 | `vector-databases.md`: demonstrate the Qdrant 6335 reachability probes and the client-API key check against a live distributed cluster in both the exposed and fixed states. They ship marked reasoned (#63) because the authoring environment has no distributed Qdrant; stand up two peers with 6335 published, then firewalled to peers, and confirm the non-peer refusal, the peer connection, and the without-key 401 / with-key 200 (M, M) | `[gap]` |
| 1.47 | `traefik.md`: demonstrate the exposedByDefault Verify probes against a live Docker and Traefik v3 deployment in the exposed and fixed states. They ship marked reasoned (#64) because the authoring environment has no container runtime; stand up Traefik with the section-1 config and an unlabelled `traefik/whoami` canary on its network, then confirm the canary returns 200 with its own `Hostname:` body at `exposedByDefault: true` and 404 at false, confirm the app baseline returns 401 unauthenticated (router plus auth present), and confirm curl 7.75.0 or newer on the target for `%{exitcode}`/`%{errormsg}` (M, M) | `[gap]` |
| 1.50 | `model-servers.md`: demonstrate the text-generation-webui `/v1/models` probe against a live instance in both the exposed and protected states. It ships marked reasoned (#69) because the authoring environment has no container runtime; stand up text-generation-webui with `--api`, confirm `/v1/models` returns `200` with no key and `401` once `--api-key` is set, and confirm the `validate_host_header` `400` when a non-localhost `Host` is forwarded without `--listen` (M, M) | `[gap]` |
| 1.71 | Demonstrate the cross-user isolation Verify steps added in #109 against live deployments: a multi-tenant pgvector table (`vector-databases.md`, confirm tenant B's role sees only B's rows and an RLS-bypassing role sees tenant A's rows) and a multi-user RAG or chat (`chat-uis.md`, confirm user B does not retrieve user A's documents), in both the exposed and fixed states. They ship marked reasoned because the authoring environment has neither (M, M) | `[gap]` |
| 1.72 | Demonstrate the `docker.md` DOCKER-USER restriction and the Docker 28.0 publishing behavior live (added #116): with a routable-interface publish and the `--ctstate NEW ! -s <subnet>` rule loaded, confirm the port connects from inside the subnet and times out from outside while a container keeps its outbound access; and confirm that a loopback-mapped port is reachable from a neighbour host on a pre-28.0 engine and not on 28.0 or later. Ships with an inline reasoned check because the authoring environment has no container runtime (M, M) | `[gap]` |
| 1.75 | `mcp-servers.md`: demonstrate the Option A wrong-audience Verify probe against a live MCP authorization server in both the exposed and fixed states, in the 2026-07-28 shape (a `server/discover` POST carrying `MCP-Protocol-Version`/`Mcp-Method` and the `_meta` protocol version). It ships marked reasoned (#174, re-shaped by the 2026-07-28 re-pin) because the authoring environment has no authorization server with a second registered resource; register a second resource, obtain an otherwise valid unexpired token issued only for it, and confirm the MCP server accepts it with audience validation absent and returns HTTP 401 with validation enforced, distinguishing the 401 from a `-32020` header mismatch; demonstrate the other two re-shaped probes (unauthenticated discover; Origin positive/negative pair) in the same session; record the server's protocol version and the results (M, M) | `[gap]` |
| 1.76 | `mlflow.md`: demonstrate the backend authentication and authorization Verify checks against a live MLflow basic-auth server in both the exposed and fixed states. They ship marked reasoned (#187) because the authoring environment has no MLflow instance; stand up `mlflow server --app-name basic-auth` with a bootstrap admin password of at least 12 characters, then confirm the backend anonymous `experiments/search` returns `401` (and `200` when auth is disabled), the admin-password `200` positive control, the wrong-password `401`, and the admin-`200` versus low-permission-`403` `experiments/get` pair on a real experiment, sending the allowed `Host` header (M, M) | `[gap]` |
| 1.83 | `mcp-servers.md`: cover the 2026-07-28 client-side authorization obligations that the server-operator scope of the guide defers: RFC 9207 authorization-response `iss` validation and mix-up defence (client MUST validate before redeeming the code), RFC 9728 AS-metadata issuer validation (metadata `issuer` MUST equal the well-known-URL issuer or the document MUST NOT be used), Client ID Metadata Documents adoption with `application_type` on any DCR fallback, client refresh-token confidentiality and `offline_access` request handling, and SSRF mitigations in OAuth discovery, either as a bounded client-hygiene subsection in `oidc-integration.md` or as a new client-side guide, whichever CONTRIBUTING's scope rules support (M, M) | `[gap]` |
| 1.84 | `mcp-servers.md`: cover proxying the 2026-07-28 `subscriptions/listen` POST response stream and the new response-caching rules: the guide's nginx blocks do not address long-lived SSE responses (buffering, `X-Accel-Buffering: no`, read timeouts), and the caching utility's `cacheScope` rule (a private-scope response MUST NOT be shared across authorization contexts; caching metadata does not replace access control) is a fronting-cache hazard for operators; verify the header names and directives at the live nginx and MCP pages before writing (M, S) | `[gap]` |
| 1.85 | Follow up in `exposure-index.md` after `sqlite-http-frontends.md` lands: add Datasette's CLI default 8001 beside the existing Triton entry in the 8001/8002 row, preserving Triton; add sqlite-web to the 8080 row. Mark both CLI listeners loopback-by-default, identify an overridden bind or published mapping as the exposure, and link the new guide. Do not change the index in the guide PR (M, XS) | `[gap]` |
## Priority 3: Add missing content

Gaps from the same audit, one row per missing guide. A gap raised by more than one family is
marked, and those are the ones worth taking first.

| ID | Item | Tags |
| --- | --- | --- |

## Priority 4: Tooling and process

| ID | Item | Tags |
| --- | --- | --- |
| 3.14 | Convention 2, the general guard grammar: gate a probe that escapes its guard for the shapes the sentinel-scoped C2 rule cannot reach without false positives (if/elif and `-z` sentinel guards, flag-variable guards, renamed sentinels). The corpus idiom is guard-then-early-exit, the probe correctly after `esac`, which is lexically indistinguishable from a warn-only guard, so any lexical rule cries wolf; `check_guard_conventions.py --strict-guards` is the manual-audit tool. Deferred from 3.11, whose gate registered `--no-c2` because the corpus uses the exit-then-probe variant the sentinel rule over-flags (M, S) | `[gap]` |

## Decisions

Maintainer rulings moved to `DECISIONS.md` on 2026-09-13, in preparation for the OPF operational-files migration (row 3.8). A row may cite a decision there; this file no longer carries them.

## Priority 5: Site and adopters

| ID | Item | Tags |
| --- | --- | --- |

## Blocked upstream

| ID | Item | Waiting on |
| --- | --- | --- |
| 3.6 | Activate the AIQT hooks: `.claude/settings.json` is classifier-gated and `tools/gen_aiqt_settings.py` merges rather than overwrites | the guardrails versioning work |
| 3.7 | Re-pin `.aiqt/` to a tag: currently pinned to a `main` commit because the only tag predates the commits this repository depends on | guardrails publishing a tag |
| 3.8 | Adopt the DevProcess / OPF operational-files standard. Deferred by the maintainer on 2026-09-12; `TODO.md` stays at the repository root and the adoption script will ingest it. See the note below | the OPF tooling release |

### On 3.8, so it is not re-derived

Read from `.aiqt/core/opf/OPF-SPEC.md` and `OPF-QUICKSTART.md` rather than from a summary:

- There is **no TODO.md to DONE.md migration** under OPF. Both are deterministic generated views
  rendered from one store of versioned TOML records. An item is a `backlog_item` record; closing
  it is a state transition (`open` to `active` to `done`, or to `dropped` when declined) plus one
  worklog entry. Nothing hand-edits a generated view. The two files in this repository today are
  hand-kept and will be imported, not converted in place.
- Ids are permanent and never reused, which is the convention this file already follows.
- A declined item stays a record in the terminal `dropped` state with its reason, which is why
  `DONE.md` carries dropped rows rather than deleting them.
- Default store location is `.working/`, with `CHANGELOG.md` and `VERSION` staying at the product
  root and `VERSION` becoming generated. The maintainer has directed that this file stay at the
  root meanwhile; guardrails has captured that as an adoption-tooling requirement.
- The tooling ships in a later pack release. Adopting by hand today would mean maintaining a TOML
  store **and** rendering its views by hand with no `opf doctor` to catch drift between them,
  which is two hand-kept copies of one truth and the defect class this repository already runs
  three freshness gates against.
